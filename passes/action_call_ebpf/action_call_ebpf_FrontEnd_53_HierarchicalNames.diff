--- before_pass
+++ after_pass
@@ -9,7 +9,7 @@ parser prs(packet_in p, out Headers_t he
 }
 control pipe(inout Headers_t headers, out bool pass) {
     bool x_0;
-    @name("Reject") action Reject(bool rej) {
+    @name("pipe.Reject") action Reject(bool rej) {
         pass = rej;
     }
     apply {
