--- before_pass
+++ after_pass
@@ -9,8 +9,8 @@ parser prs(packet_in p, out Headers_t he
 }
 control pipe(inout Headers_t headers, out bool pass) {
     bool x_0;
-    @name("Reject") action Reject(bool rej_0) {
-        pass = rej_0;
+    @name("Reject") action Reject(bool rej) {
+        pass = rej;
     }
     apply {
         x_0 = true;
