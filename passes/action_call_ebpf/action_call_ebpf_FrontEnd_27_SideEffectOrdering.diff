--- before_pass
+++ after_pass
@@ -10,11 +10,17 @@ parser prs(packet_in p, out Headers_t he
 control pipe(inout Headers_t headers, out bool pass) {
     bool x_0;
     @name("Reject") action Reject_0(bool rej) {
-        pass = rej;
+        {
+            pass = rej;
+        }
     }
     apply {
-        x_0 = true;
-        Reject_0(x_0);
+        {
+            x_0 = true;
+        }
+        {
+            Reject_0(x_0);
+        }
     }
 }
 ebpfFilter<Headers_t>(prs(), pipe()) main;
