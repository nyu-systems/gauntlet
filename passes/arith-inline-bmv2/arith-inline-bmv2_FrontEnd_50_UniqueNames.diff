--- before_pass
+++ after_pass
@@ -34,17 +34,17 @@ control deparser(packet_out b, in Header
     }
 }
 control ingress(inout Headers h, inout Meta m, inout standard_metadata_t sm) {
-    @name("c.add") action c_add() {
+    @name("c.add") action c_add_0() {
         h.h.c = (bit<64>)(h.h.a + h.h.b);
     }
-    @name("c.t") table c_t_0 {
+    @name("c.t") table c_t {
         actions = {
-            c_add();
+            c_add_0();
         }
-        const default_action = c_add();
+        const default_action = c_add_0();
     }
     apply {
-        c_t_0.apply();
+        c_t.apply();
         sm.egress_spec = 9w0;
     }
 }
