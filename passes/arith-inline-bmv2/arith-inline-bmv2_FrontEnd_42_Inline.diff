--- before_pass
+++ after_pass
@@ -5,20 +5,6 @@ header hdr {
     bit<32> b;
     bit<64> c;
 }
-control compute(inout hdr h) {
-    @name("add") action add_0() {
-        h.c = (bit<64>)(h.a + h.b);
-    }
-    @name("t") table t_0 {
-        actions = {
-            add_0();
-        }
-        const default_action = add_0();
-    }
-    apply {
-        t_0.apply();
-    }
-}
 struct Headers {
     hdr h;
 }
@@ -48,9 +34,19 @@ control deparser(packet_out b, in Header
     }
 }
 control ingress(inout Headers h, inout Meta m, inout standard_metadata_t sm) {
-    @name("c") compute() c_0;
+    @name("c.add") action c_add() {
+        h.h.c = (bit<64>)(h.h.a + h.h.b);
+    }
+    @name("c.t") table c_t {
+        actions = {
+            c_add();
+        }
+        const default_action = c_add();
+    }
     apply {
-        c_0.apply(h.h);
+        {
+            c_t.apply();
+        }
         sm.egress_spec = 9w0;
     }
 }
