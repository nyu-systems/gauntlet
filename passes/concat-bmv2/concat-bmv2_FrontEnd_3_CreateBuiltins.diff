--- before_pass
+++ after_pass
@@ -40,9 +40,9 @@ control ingress(inout Headers h, inout M
     }
     table t {
         actions = {
-            concat;
+            concat();
         }
-        const default_action = concat;
+        const default_action = concat();
     }
     apply {
         t.apply();
