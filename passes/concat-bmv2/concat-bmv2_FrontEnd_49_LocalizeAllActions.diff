--- before_pass
+++ after_pass
@@ -34,15 +34,15 @@ control deparser(packet_out b, in Header
     }
 }
 control ingress(inout Headers h, inout Meta m, inout standard_metadata_t sm) {
-    @name("concat") action concat() {
+    @name("concat") action concat_0() {
         h.h.c = h.h.a ++ h.h.b;
         sm.egress_spec = 9w0;
     }
     @name("t") table t {
         actions = {
-            concat();
+            concat_0();
         }
-        const default_action = concat();
+        const default_action = concat_0();
     }
     apply {
         t.apply();
