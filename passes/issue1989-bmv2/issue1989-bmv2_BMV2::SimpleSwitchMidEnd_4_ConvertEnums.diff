--- before_pass
+++ after_pass
@@ -6,10 +6,10 @@ enum meter_color_t {
     COLOR_YELLOW
 }
 struct test_metadata_t {
-    bit<32>       color;
-    meter_color_t enum_color;
-    bit<32>       other_metadata;
-    bit<16>       smaller_metadata;
+    bit<32> color;
+    bit<32> enum_color;
+    bit<32> other_metadata;
+    bit<16> smaller_metadata;
 }
 header ethernet_t {
     bit<48> dstAddr;
