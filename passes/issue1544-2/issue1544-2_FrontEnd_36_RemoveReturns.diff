--- before_pass
+++ after_pass
@@ -1,11 +1,17 @@
 bit<32> min(in bit<32> a, in bit<32> b) {
+    bool hasReturned = false;
+    bit<32> retval;
     bit<32> tmp;
     if (a > b) {
         tmp = b;
     } else {
         tmp = a;
     }
-    return tmp;
+    {
+        hasReturned = true;
+        retval = tmp;
+    }
+    return retval;
 }
 control c(inout bit<32> x) {
     bit<32> tmp_0;
