--- before_pass
+++ after_pass
@@ -4,11 +4,6 @@ extern void extern_func(out bit<32> d, b
 header hdr {
     bit<32> a;
 }
-control compute(inout hdr h) {
-    apply {
-        extern_func(h.a, 32w0xff);
-    }
-}
 struct Headers {
     hdr h;
 }
@@ -38,9 +33,10 @@ control deparser(packet_out b, in Header
     }
 }
 control ingress(inout Headers h, inout Meta m, inout standard_metadata_t sm) {
-    @name("c") compute() c_0;
     apply {
-        c_0.apply(h.h);
+        {
+            extern_func(h.h.a, 32w0xff);
+        }
         sm.egress_spec = 9w0;
     }
 }
