--- before_pass
+++ after_pass
@@ -20,20 +20,25 @@ parser IngressParserImpl(packet_in pkt,
     }
 }
 control cIngress(inout headers_t hdr, inout metadata_t user_meta, in psa_ingress_input_metadata_t istd, inout psa_ingress_output_metadata_t ostd) {
-    @name("resubmit") action resubmit_1() {
+    @name(".send_to_port") action send_to_port_1(inout psa_ingress_output_metadata_t meta_0, in PortId_t egress_port_0) {
+        meta_0.drop = false;
+        meta_0.multicast_group = (MulticastGroup_t)32w0;
+        meta_0.egress_port = egress_port_0;
+    }
+    @name("resubmit") action resubmit_0() {
         hdr.ethernet.srcAddr = 48w256;
         ostd.resubmit = true;
     }
-    @name("pkt_write") action pkt_write() {
+    @name("pkt_write") action pkt_write_0() {
         ostd.drop = false;
         hdr.ethernet.dstAddr = 48w4;
     }
     apply {
-        pkt_write();
+        pkt_write_0();
         if (istd.packet_path != PSA_PacketPath_t.RESUBMIT) {
-            resubmit_1();
+            resubmit_0();
         } else {
-            send_to_port(ostd, (PortId_t)(PortIdUint_t)hdr.ethernet.dstAddr);
+            send_to_port_1(ostd, (PortId_t)(PortIdUint_t)hdr.ethernet.dstAddr);
         }
     }
 }
