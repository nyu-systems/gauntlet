--- before_pass
+++ after_pass
@@ -32,9 +32,9 @@ control ingressImpl(inout headers_t hdr,
             hdr.ethernet.dstAddr: exact;
         }
         actions = {
-            do_meter;
+            do_meter();
         }
-        const default_action = do_meter;
+        const default_action = do_meter();
     }
     apply {
         stdmeta.egress_spec = 1;
