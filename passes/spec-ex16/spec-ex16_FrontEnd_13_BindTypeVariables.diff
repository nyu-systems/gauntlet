--- before_pass
+++ after_pass
@@ -15,5 +15,5 @@ control Map2(in bit<8> d) {
     apply {
     }
 }
-Switch(P(), Map1()) main;
+Switch<bit<32>>(P(), Map1()) main;
 Switch<bit<32>>(P(), Map1()) main1;
