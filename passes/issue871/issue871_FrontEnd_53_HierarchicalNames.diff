--- before_pass
+++ after_pass
@@ -3,7 +3,7 @@ extern lpf<I> {
     T execute<T>(in T data, in I index);
 }
 control c() {
-    @name("lpf_0") lpf<bit<32>>(32w32) lpf_1;
+    @name("c.lpf_0") lpf<bit<32>>(32w32) lpf_1;
     apply {
         lpf_1.execute<bit<8>>(8w0, 32w0);
     }
