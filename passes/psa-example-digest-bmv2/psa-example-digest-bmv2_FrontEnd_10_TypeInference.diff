--- before_pass
+++ after_pass
@@ -1,13 +1,13 @@
 #include <core.p4>
 #include <psa.p4>
 enum bit<16> EthTypes {
-    IPv4 = 0x800,
-    ARP = 0x806,
-    RARP = 0x8035,
-    EtherTalk = 0x809b,
-    VLAN = 0x8100,
-    IPX = 0x8137,
-    IPv6 = 0x86dd
+    IPv4 = 16w0x800,
+    ARP = 16w0x806,
+    RARP = 16w0x8035,
+    EtherTalk = 16w0x809b,
+    VLAN = 16w0x8100,
+    IPX = 16w0x8137,
+    IPv6 = 16w0x86dd
 }
 typedef bit<48> EthernetAddress;
 header ethernet_t {
@@ -51,7 +51,7 @@ parser CommonParser(packet_in buffer, ou
     state parse_ethernet {
         buffer.extract(parsed_hdr.ethernet);
         transition select(parsed_hdr.ethernet.etherType) {
-            0x800: parse_ipv4;
+            16w0x800: parse_ipv4;
             default: accept;
         }
     }
