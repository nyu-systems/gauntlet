--- before_pass
+++ after_pass
@@ -35,11 +35,13 @@ control MyIngress(inout my_packet p, ino
     }
     table t {
         actions = {
-            set_data;
+            set_data();
+            @defaultonly NoAction();
         }
         key = {
             meta.data[0].da: exact;
         }
+        default_action = NoAction();
     }
     apply {
         t.apply();
