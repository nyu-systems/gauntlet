--- before_pass
+++ after_pass
@@ -4,8 +4,12 @@ package top(proto _p);
 control c() {
     tuple<bit<32>, bool> x_0;
     apply {
-        x_0 = { 32w10, false };
-        f<tuple<bit<32>, bool>>(x_0);
+        {
+            x_0 = { 32w10, false };
+        }
+        {
+            f<tuple<bit<32>, bool>>(x_0);
+        }
     }
 }
 top(c()) main;
