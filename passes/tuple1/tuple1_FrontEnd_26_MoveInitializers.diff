--- before_pass
+++ after_pass
@@ -2,8 +2,9 @@ extern void f<T>(in T data);
 control proto();
 package top(proto _p);
 control c() {
-    tuple<bit<32>, bool> x_0 = { 32w10, false };
+    tuple<bit<32>, bool> x_0;
     apply {
+        x_0 = { 32w10, false };
         f<tuple<bit<32>, bool>>(x_0);
     }
 }
