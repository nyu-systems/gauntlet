--- before_pass
+++ after_pass
@@ -2,9 +2,9 @@ extern void f<T>(in T data);
 control proto();
 package top(proto _p);
 control c() {
-    tuple<bit<32>, bool> x = { 32w10, false };
+    tuple<bit<32>, bool> x_0 = { 32w10, false };
     apply {
-        f<tuple<bit<32>, bool>>(x);
+        f<tuple<bit<32>, bool>>(x_0);
     }
 }
 top(c()) main;
