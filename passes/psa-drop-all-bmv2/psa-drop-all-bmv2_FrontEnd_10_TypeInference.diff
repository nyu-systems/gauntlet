--- before_pass
+++ after_pass
@@ -38,7 +38,7 @@ parser IngressParserImpl(packet_in buffe
     state parse_ethernet {
         buffer.extract(parsed_hdr.ethernet);
         transition select(parsed_hdr.ethernet.etherType) {
-            0x800: parse_ipv4;
+            16w0x800: parse_ipv4;
             default: accept;
         }
     }
