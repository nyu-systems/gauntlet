--- before_pass
+++ after_pass
@@ -6,9 +6,7 @@ struct metadata {
 }
 parser MyParser(packet_in packet, out headers hdr, inout metadata meta, inout standard_metadata_t standard_metadata) {
     state start {
-        {
-            packet.advance(32w8);
-        }
+        packet.advance(32w8);
         transition accept;
     }
 }
