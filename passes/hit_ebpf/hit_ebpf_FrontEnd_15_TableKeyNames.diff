--- before_pass
+++ after_pass
@@ -45,7 +45,7 @@ control pipe(inout Headers_t headers, ou
     }
     table Check_src_ip {
         key = {
-            headers.ipv4.srcAddr: exact;
+            headers.ipv4.srcAddr: exact @name("headers.ipv4.srcAddr") ;
         }
         actions = {
             Reject();
