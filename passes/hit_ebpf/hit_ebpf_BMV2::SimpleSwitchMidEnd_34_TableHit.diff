--- before_pass
+++ after_pass
@@ -66,7 +66,11 @@ control pipe(inout Headers_t headers, ou
             hasReturned = true;
         }
         if (!hasReturned) {
-            tmp = Check_src_ip_0.apply().hit;
+            if (Check_src_ip_0.apply().hit) {
+                tmp = true;
+            } else {
+                tmp = false;
+            }
         }
     }
 }
