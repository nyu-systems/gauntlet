--- before_pass
+++ after_pass
@@ -60,9 +60,7 @@ control pipe(inout Headers_t headers, ou
         pass = true;
         if (!headers.ipv4.isValid()) {
             pass = false;
-            {
-                hasReturned = true;
-            }
+            hasReturned = true;
         }
         if (!hasReturned) {
             tmp = Check_src_ip_0.apply().hit;
