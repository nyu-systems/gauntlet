--- before_pass
+++ after_pass
@@ -1,7 +1,7 @@
 #include <core.p4>
 #include <v1model.p4>
 bit<16> incr(in bit<16> x) {
-    return x + 1;
+    return x + 16w1;
 }
 bit<16> twoxplus1(in bit<16> x) {
     return x + incr(x);
