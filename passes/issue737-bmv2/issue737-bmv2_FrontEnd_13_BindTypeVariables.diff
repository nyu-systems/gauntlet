--- before_pass
+++ after_pass
@@ -44,4 +44,4 @@ control TopDeparser(packet_out b, in Par
     apply {
     }
 }
-V1Switch(TopParser(), VeryChecksum(), IngressP(), EgressP(), ChecksumComputer(), TopDeparser()) main;
+V1Switch<Parsed_packet, Meta>(TopParser(), VeryChecksum(), IngressP(), EgressP(), ChecksumComputer(), TopDeparser()) main;
