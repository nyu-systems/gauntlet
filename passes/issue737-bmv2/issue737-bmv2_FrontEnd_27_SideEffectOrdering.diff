--- before_pass
+++ after_pass
@@ -11,7 +11,9 @@ struct Parsed_packet {
 }
 parser TopParser(packet_in b, out Parsed_packet p, inout Meta m, inout standard_metadata_t metadata) {
     state start {
-        m.metafield = true;
+        {
+            m.metafield = true;
+        }
         transition accept;
     }
 }
