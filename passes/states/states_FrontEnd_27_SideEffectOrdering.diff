--- before_pass
+++ after_pass
@@ -11,7 +11,9 @@ struct Headers_t {
 }
 parser prs(packet_in p, out Headers_t headers) {
     state start {
-        p.extract<Ethernet_h>(headers.ethernet);
+        {
+            p.extract<Ethernet_h>(headers.ethernet);
+        }
         transition select(headers.ethernet.etherType) {
             16w0x800: ip;
             default: reject;
