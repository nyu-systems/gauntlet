--- before_pass
+++ after_pass
@@ -39,14 +39,14 @@ control deparser(packet_out b, in Header
     }
 }
 control ingress(inout Headers h, inout Meta m, inout standard_metadata_t sm) {
-    @name("set_port") action set_port() {
+    @name("set_port") action set_port_0() {
         sm.egress_spec = (bit<9>)m.v;
     }
     @name("t") table t {
         actions = {
-            set_port();
+            set_port_0();
         }
-        const default_action = set_port();
+        const default_action = set_port_0();
     }
     apply {
         t.apply();
