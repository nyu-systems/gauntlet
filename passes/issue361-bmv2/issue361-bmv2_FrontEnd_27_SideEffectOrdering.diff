--- before_pass
+++ after_pass
@@ -11,14 +11,18 @@ struct my_metadata {
 parser MyParser(packet_in b, out my_packet p, inout my_metadata m, inout standard_metadata_t s) {
     bool bv_0;
     state start {
-        bv_0 = true;
+        {
+            bv_0 = true;
+        }
         transition select(bv_0) {
             false: next;
             true: accept;
         }
     }
     state next {
-        b.extract<H>(p.h);
+        {
+            b.extract<H>(p.h);
+        }
         transition accept;
     }
 }
