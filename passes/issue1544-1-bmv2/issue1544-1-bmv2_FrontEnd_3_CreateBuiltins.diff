--- before_pass
+++ after_pass
@@ -40,7 +40,7 @@ control ingress(inout headers hdr, inout
             hdr.ethernet.dstAddr: exact;
         }
         actions = {
-            set_port;
+            set_port();
             my_drop(standard_metadata);
         }
         default_action = my_drop(standard_metadata);
