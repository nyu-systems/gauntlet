--- before_pass
+++ after_pass
@@ -15,7 +15,6 @@ parser ParserI(packet_in pk, out H hdr,
     }
 }
 control IngressI(inout H hdr, inout M meta, inout standard_metadata_t smeta) {
-    bool b_0;
     apply {
         hdr.h.isValid();
     }
