--- before_pass
+++ after_pass
@@ -14,8 +14,6 @@ parser ParserI(packet_in pk, out H hdr,
         transition accept;
     }
 }
-action empty() {
-}
 control IngressI(inout H hdr, inout M meta, inout standard_metadata_t smeta) {
     apply {
         bool b = hdr.h.isValid();
