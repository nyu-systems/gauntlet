--- before_pass
+++ after_pass
@@ -6,10 +6,10 @@ extern e<T> {
 parser simple(in bit<2> a);
 package m(simple n);
 parser p1_0(in bit<2> a) {
-    bit<2> tmp_0;
-    @name("ei") e<bit<2>>() ei;
+    bit<2> tmp;
+    @name("ei") e<bit<2>>() ei_0;
     state start {
-        tmp_0 = ei.get();
+        tmp = ei_0.get();
         transition accept;
     }
 }
