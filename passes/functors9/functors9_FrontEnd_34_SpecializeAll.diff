--- before_pass
+++ after_pass
@@ -3,15 +3,14 @@ extern e<T> {
     e();
     T get();
 }
-parser p1<T>(in T a) {
-    T w_0;
-    T tmp;
-    @name("ei") e<T>() ei_0;
+parser simple(in bit<2> a);
+package m(simple n);
+parser p1_0(in bit<2> a) {
+    bit<2> tmp;
+    @name("ei") e<bit<2>>() ei_0;
     state start {
         tmp = ei_0.get();
         transition accept;
     }
 }
-parser simple(in bit<2> a);
-package m(simple n);
-m(p1<bit<2>>()) main;
+m(p1_0()) main;
