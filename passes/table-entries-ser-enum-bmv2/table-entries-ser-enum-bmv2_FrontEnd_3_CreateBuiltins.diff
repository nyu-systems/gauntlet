--- before_pass
+++ after_pass
@@ -53,8 +53,8 @@ control ingress(inout Header_t h, inout
             h.h.f2: ternary;
         }
         actions = {
-            a;
-            a_with_control_params;
+            a();
+            a_with_control_params();
         }
         const default_action = a();
         const entries = {
