--- before_pass
+++ after_pass
@@ -11,10 +11,11 @@ header_union U {
 control ct(out bit<32> b);
 package top(ct _ct);
 control c(out bit<32> x) {
+    U u_0;
+    U[2] u2_0;
+    bool b_0;
     apply {
-        U u_0;
-        U[2] u2_0;
-        bool b_0 = u_0.isValid();
+        b_0 = u_0.isValid();
         b_0 = b_0 || u_0.h1.isValid();
         x = u_0.h1.f + u_0.h2.g;
         u_0.h1.setValid();
