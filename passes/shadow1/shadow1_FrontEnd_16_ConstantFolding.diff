--- before_pass
+++ after_pass
@@ -3,7 +3,7 @@ extern counter {
 parser p() {
     bit<16> counter;
     state start {
-        counter = (bit<16>)16w0;
+        counter = 16w0;
         transition accept;
     }
 }
