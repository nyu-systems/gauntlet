--- before_pass
+++ after_pass
@@ -3,7 +3,7 @@ extern counter {
 parser p() {
     bit<16> counter;
     state start {
-        counter = 0;
+        counter = (bit<16>)16w0;
         transition accept;
     }
 }
