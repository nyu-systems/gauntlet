--- before_pass
+++ after_pass
@@ -1,7 +1,7 @@
 extern void f(in bit<16> x, in bool y);
 control c() {
     apply {
-        bit<16> xv = 0;
+        bit<16> xv = (bit<16>)16w0;
         bool b = true;
         f(y = b, x = xv);
     }
