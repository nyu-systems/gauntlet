--- before_pass
+++ after_pass
@@ -8,7 +8,7 @@ parser prs(packet_in p, out Headers_t he
     }
 }
 control pipe(inout Headers_t headers, out bool pass) {
-    @name("Reject") action Reject(bit<8> rej, bit<8> bar) {
+    @name("pipe.Reject") action Reject(bit<8> rej, bit<8> bar) {
         if (rej == 8w0) {
             pass = true;
         } else {
@@ -18,7 +18,7 @@ control pipe(inout Headers_t headers, ou
             pass = false;
         }
     }
-    @name("t") table t_0 {
+    @name("pipe.t") table t_0 {
         actions = {
             Reject();
         }
