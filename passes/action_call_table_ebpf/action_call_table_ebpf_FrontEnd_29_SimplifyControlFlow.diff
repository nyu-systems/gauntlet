--- before_pass
+++ after_pass
@@ -25,9 +25,7 @@ control pipe(inout Headers_t headers, ou
         default_action = Reject_0(8w1, 8w0);
     }
     apply {
-        {
-            t_0.apply();
-        }
+        t_0.apply();
     }
 }
 ebpfFilter<Headers_t>(prs(), pipe()) main;
