--- before_pass
+++ after_pass
@@ -8,7 +8,7 @@ parser prs(packet_in p, out Headers_t he
     }
 }
 control pipe(inout Headers_t headers, out bool pass) {
-    @name("Reject") action Reject_0(bit<8> rej, bit<8> bar) {
+    @name("Reject") action Reject(bit<8> rej, bit<8> bar) {
         if (rej == 8w0) {
             pass = true;
         } else {
@@ -18,14 +18,14 @@ control pipe(inout Headers_t headers, ou
             pass = false;
         }
     }
-    @name("t") table t {
+    @name("t") table t_0 {
         actions = {
-            Reject_0();
+            Reject();
         }
-        default_action = Reject_0(8w1, 8w0);
+        default_action = Reject(8w1, 8w0);
     }
     apply {
-        t.apply();
+        t_0.apply();
     }
 }
 ebpfFilter<Headers_t>(prs(), pipe()) main;
