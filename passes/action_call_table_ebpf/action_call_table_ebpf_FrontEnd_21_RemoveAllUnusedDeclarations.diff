--- before_pass
+++ after_pass
@@ -25,7 +25,6 @@ control pipe(inout Headers_t headers, ou
         default_action = Reject(8w1, 8w0);
     }
     apply {
-        bool x = true;
         t.apply();
     }
 }
