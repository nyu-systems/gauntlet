--- before_pass
+++ after_pass
@@ -15,8 +15,8 @@ control c(bit<1> x) {
             x: exact;
         }
         actions = {
-            Set_dmac;
-            drop;
+            Set_dmac();
+            drop();
         }
         default_action = Set_dmac((EthernetAddress)48w0xaabbccddeeff);
         implementation = tbl();
