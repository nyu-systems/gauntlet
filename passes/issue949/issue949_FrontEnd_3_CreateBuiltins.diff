--- before_pass
+++ after_pass
@@ -46,8 +46,10 @@ control ingress(inout headers hdr, inout
             hdr.ethernet.srcAddr: exact;
         }
         actions = {
-            setDest;
+            setDest();
+            @defaultonly NoAction();
         }
+        default_action = NoAction();
     }
     apply {
         bool didHit = someTable.apply().hit;
