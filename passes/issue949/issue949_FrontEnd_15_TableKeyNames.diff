--- before_pass
+++ after_pass
@@ -43,7 +43,7 @@ control ingress(inout headers hdr, inout
     }
     table someTable {
         key = {
-            hdr.ethernet.srcAddr: exact;
+            hdr.ethernet.srcAddr: exact @name("hdr.ethernet.srcAddr") ;
         }
         actions = {
             setDest();
