--- before_pass
+++ after_pass
@@ -48,8 +48,9 @@ control ingress(inout headers hdr, inout
             user_meta.fwd_meta.x4           : exact;
         }
         actions = {
-            NoAction;
+            NoAction();
         }
+        default_action = NoAction();
     }
     bit<16> tmp;
     bit<32> x1;
