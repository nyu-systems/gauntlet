--- before_pass
+++ after_pass
@@ -95,7 +95,9 @@ control computeChecksum(inout headers hd
     }
 }
 control egress(inout headers hdr, inout metadata meta, inout standard_metadata_t standard_metadata) {
-    @name("_drop") action _drop() {
+    @name(".NoAction") action NoAction_1() {
+    }
+    @name("_drop") action _drop_0() {
         mark_to_drop(standard_metadata);
     }
     @name("drop_tbl") table drop_tbl {
@@ -103,11 +105,11 @@ control egress(inout headers hdr, inout
             meta.local_metadata.set_drop: exact @name("meta.ingress_metadata.set_drop") ;
         }
         actions = {
-            _drop();
-            NoAction();
+            _drop_0();
+            NoAction_1();
         }
         size = 2;
-        default_action = NoAction();
+        default_action = NoAction_1();
     }
     apply {
         drop_tbl.apply();
@@ -115,17 +117,17 @@ control egress(inout headers hdr, inout
 }
 control ingress(inout headers hdr, inout metadata meta, inout standard_metadata_t standard_metadata) {
     @name("registerRound") register<bit<16>>(32w65536) registerRound;
-    @name("read_round") action read_round() {
+    @name("read_round") action read_round_0() {
         registerRound.read(meta.local_metadata.round, hdr.myhdr.inst);
     }
     @name("round_tbl") table round_tbl {
         key = {
         }
         actions = {
-            read_round();
+            read_round_0();
         }
         size = 8;
-        default_action = read_round();
+        default_action = read_round_0();
     }
     apply {
         if (hdr.ipv4.isValid()) {
