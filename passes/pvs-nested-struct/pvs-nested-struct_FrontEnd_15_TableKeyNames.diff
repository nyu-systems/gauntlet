--- before_pass
+++ after_pass
@@ -43,7 +43,7 @@ control MyIngress(inout my_packet p, ino
             @defaultonly NoAction();
         }
         key = {
-            meta.data[0].da: exact;
+            meta.data[0].da: exact @name("meta.data[0].da") ;
         }
         default_action = NoAction();
     }
