--- before_pass
+++ after_pass
@@ -7,10 +7,10 @@ control c(out bit<32> x) {
     }
     table t {
         actions = {
-            a1;
-            a2;
+            a1();
+            a2();
         }
-        default_action = a1;
+        default_action = a1();
     }
     apply {
         switch (t.apply().action_run) {
