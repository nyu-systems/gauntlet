--- before_pass
+++ after_pass
@@ -25,7 +25,9 @@ control c(out bit<32> x) {
 control d(out bit<32> x) {
     @name("cinst") c() cinst_0;
     apply {
-        cinst_0.apply(x);
+        {
+            cinst_0.apply(x);
+        }
     }
 }
 control dproto(out bit<32> x);
