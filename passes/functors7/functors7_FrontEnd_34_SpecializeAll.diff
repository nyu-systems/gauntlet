--- before_pass
+++ after_pass
@@ -1,9 +1,9 @@
 #include <core.p4>
-parser p1<T>(in T a) {
+parser simple(in bit<2> a);
+package m(simple n);
+parser p1_0(in bit<2> a) {
     state start {
         transition accept;
     }
 }
-parser simple(in bit<2> a);
-package m(simple n);
-m(p1<bit<2>>()) main;
+m(p1_0()) main;
