--- before_pass
+++ after_pass
@@ -22,7 +22,7 @@ parser parserI(packet_in pkt, out Parsed
 }
 control cIngress(inout Parsed_packet hdr, inout Metadata meta, inout standard_metadata_t stdmeta) {
     apply {
-        digest(32w5, { hdr.ethernet.srcAddr });
+        digest<tuple<bit<48>>>(32w5, { hdr.ethernet.srcAddr });
         hdr.ethernet.srcAddr = (bit<48>)48w0;
     }
 }
@@ -38,4 +38,4 @@ control uc(inout Parsed_packet hdr, inou
     apply {
     }
 }
-V1Switch(parserI(), vc(), cIngress(), cEgress(), uc(), DeparserI()) main;
+V1Switch<Parsed_packet, Metadata>(parserI(), vc(), cIngress(), cEgress(), uc(), DeparserI()) main;
