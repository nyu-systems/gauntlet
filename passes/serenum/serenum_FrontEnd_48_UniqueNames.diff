--- before_pass
+++ after_pass
@@ -17,10 +17,10 @@ struct Headers {
     Ethernet eth;
 }
 parser prs(packet_in p, out Headers h) {
-    Ethernet e_0;
+    Ethernet e;
     state start {
-        p.extract<Ethernet>(e_0);
-        transition select(e_0.type) {
+        p.extract<Ethernet>(e);
+        transition select(e.type) {
             EthTypes.IPv4: accept;
             EthTypes.ARP: accept;
             default: reject;
@@ -29,11 +29,11 @@ parser prs(packet_in p, out Headers h) {
 }
 control c(inout Headers h) {
     apply {
-        bool hasReturned = false;
+        bool hasReturned_0 = false;
         if (!h.eth.isValid()) {
-            hasReturned = true;
+            hasReturned_0 = true;
         }
-        if (!hasReturned) {
+        if (!hasReturned_0) {
             if (h.eth.type == EthTypes.IPv4) {
                 h.eth.setInvalid();
             } else {
