--- before_pass
+++ after_pass
@@ -35,7 +35,7 @@ control c(inout Headers h) {
         if (h.eth.type == EthTypes.IPv4) {
             h.eth.setInvalid();
         } else {
-            h.eth.type = (EthTypes)(bit<16>)0;
+            h.eth.type = (EthTypes)16w0;
         }
     }
 }
