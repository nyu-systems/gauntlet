--- before_pass
+++ after_pass
@@ -40,6 +40,7 @@ control MyComputeChecksum(inout headers
     bit<16> tmp;
     apply {
         {
+            h_0.setValid();
             h_0 = { hdr.h.src, hdr.h.dst, 16w0 };
         }
         {
