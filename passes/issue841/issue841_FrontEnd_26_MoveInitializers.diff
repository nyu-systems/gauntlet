--- before_pass
+++ after_pass
@@ -33,9 +33,10 @@ control MyDeparser(packet_out packet, in
     }
 }
 control MyComputeChecksum(inout headers hdr, inout metadata meta) {
-    h_t h_0 = { hdr.h.src, hdr.h.dst, 16w0 };
+    h_t h_0;
     @name("checksum") Checksum16() checksum_0;
     apply {
+        h_0 = { hdr.h.src, hdr.h.dst, 16w0 };
         hdr.h.csum = checksum_0.get<h_t>(h_0);
     }
 }
