--- before_pass
+++ after_pass
@@ -15,22 +15,22 @@ control ctrl() {
         bit<32> a;
         bit<32> b;
         bit<32> c;
-        a = 0;
-        b = 1;
-        c = 2;
+        a = (bit<32>)32w0;
+        b = (bit<32>)32w1;
+        c = (bit<32>)32w2;
         switch (t.apply().action_run) {
             e: {
-                b = 2;
+                b = (bit<32>)32w2;
                 t.apply();
-                c = 3;
+                c = (bit<32>)32w3;
             }
             f: {
-                b = 3;
+                b = (bit<32>)32w3;
                 t.apply();
-                c = 4;
+                c = (bit<32>)32w4;
             }
         }
-        c = 5;
+        c = (bit<32>)32w5;
     }
 }
 control noop();
