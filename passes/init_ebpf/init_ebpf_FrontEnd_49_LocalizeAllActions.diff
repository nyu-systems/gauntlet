--- before_pass
+++ after_pass
@@ -15,7 +15,9 @@ parser prs(packet_in p, out Headers_t he
     }
 }
 control pipe(inout Headers_t headers, out bool pass) {
-    @name("match") action match(bool act) {
+    @name(".NoAction") action NoAction_1() {
+    }
+    @name("match") action match_0(bool act) {
         pass = act;
     }
     @name("tbl") table tbl {
@@ -23,15 +25,15 @@ control pipe(inout Headers_t headers, ou
             headers.ethernet.protocol: exact @name("headers.ethernet.protocol") ;
         }
         actions = {
-            match();
-            NoAction();
+            match_0();
+            NoAction_1();
         }
         const entries = {
-                        16w0x800 : match(true);
-                        16w0xd000 : match(false);
+                        16w0x800 : match_0(true);
+                        16w0xd000 : match_0(false);
         }
         implementation = hash_table(32w64);
-        default_action = NoAction();
+        default_action = NoAction_1();
     }
     apply {
         pass = true;
