--- before_pass
+++ after_pass
@@ -9,8 +9,9 @@ struct Headers {
 struct M {
 }
 parser prs(packet_in p, out Headers h, inout M meta, inout standard_metadata_t s) {
+    Header arg;
     state start {
-        p.extract<Header>(_);
+        p.extract<Header>(arg);
         p.extract<Header>(h.h);
         transition accept;
     }
