--- before_pass
+++ after_pass
@@ -79,40 +79,40 @@ parser parserI(packet_in pkt, out header
     }
 }
 control cIngress(inout headers hdr, inout metadata meta, inout standard_metadata_t stdmeta) {
-    action foo1(IPv4Address dstAddr) {
+    @name("foo1") action foo1_0(IPv4Address dstAddr) {
         hdr.ipv4.dstAddr = dstAddr;
     }
-    action foo2(IPv4Address srcAddr) {
+    @name("foo2") action foo2_0(IPv4Address srcAddr) {
         hdr.ipv4.srcAddr = srcAddr;
     }
-    table t0 {
+    @name("t0") table t0_0 {
         key = {
             hdr.tcp.dstPort: exact @name("hdr.tcp.dstPort") ;
         }
         actions = {
-            foo1();
-            foo2();
+            foo1_0();
+            foo2_0();
             @defaultonly NoAction();
         }
         size = 8;
         default_action = NoAction();
     }
-    table t1 {
+    @name("t1") table t1_0 {
         key = {
             hdr.tcp.dstPort: exact @name("hdr.tcp.dstPort") ;
         }
         actions = {
-            foo1();
-            foo2();
+            foo1_0();
+            foo2_0();
             @defaultonly NoAction();
         }
         size = 8;
         default_action = NoAction();
     }
-    table t2 {
+    @name("t2") table t2_0 {
         actions = {
-            foo1();
-            foo2();
+            foo1_0();
+            foo2_0();
             @defaultonly NoAction();
         }
         key = {
@@ -123,10 +123,10 @@ control cIngress(inout headers hdr, inou
         default_action = NoAction();
     }
     apply {
-        t0.apply();
-        t1.apply();
+        t0_0.apply();
+        t1_0.apply();
         meta.hash1 = hdr.ipv4.dstAddr[15:0];
-        t2.apply();
+        t2_0.apply();
     }
 }
 control cEgress(inout headers hdr, inout metadata meta, inout standard_metadata_t stdmeta) {
