--- before_pass
+++ after_pass
@@ -91,7 +91,7 @@ control cIngress(inout headers hdr, inou
     }
     table t0 {
         key = {
-            hdr.tcp.dstPort: exact;
+            hdr.tcp.dstPort: exact @name("hdr.tcp.dstPort") ;
         }
         actions = {
             foo1();
@@ -103,7 +103,7 @@ control cIngress(inout headers hdr, inou
     }
     table t1 {
         key = {
-            hdr.tcp.dstPort: exact;
+            hdr.tcp.dstPort: exact @name("hdr.tcp.dstPort") ;
         }
         actions = {
             foo1();
@@ -120,8 +120,8 @@ control cIngress(inout headers hdr, inou
             @defaultonly NoAction();
         }
         key = {
-            hdr.tcp.srcPort: exact;
-            meta.hash1     : selector;
+            hdr.tcp.srcPort: exact @name("hdr.tcp.srcPort") ;
+            meta.hash1     : selector @name("meta.hash1") ;
         }
         size = 16;
         default_action = NoAction();
