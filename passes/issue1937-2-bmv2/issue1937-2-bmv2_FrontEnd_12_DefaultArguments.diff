--- before_pass
+++ after_pass
@@ -20,7 +20,7 @@ parser parserImpl(packet_in packet, out
     @name("foo") foo() foo_inst_0;
     state start {
         foo_inst.apply(hdr.h1.f1, hdr.h1.f1);
-        foo_inst_0.apply(hdr.h1.f2);
+        foo_inst_0.apply(x = hdr.h1.f2, y = 5);
         transition accept;
     }
 }
