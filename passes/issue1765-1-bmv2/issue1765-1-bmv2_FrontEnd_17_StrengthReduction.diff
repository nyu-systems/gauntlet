--- before_pass
+++ after_pass
@@ -122,7 +122,7 @@ parser MyParser(packet_in packet, out he
     }
     state ipv4 {
         packet.extract<ipv4_t>(hdr.ipv4);
-        meta.tcp_length = hdr.ipv4.totalLen - 16w20;
+        meta.tcp_length = hdr.ipv4.totalLen + 16w65516;
         transition select(hdr.ipv4.protocol) {
             8w6: tcp;
             8w17: udp;
