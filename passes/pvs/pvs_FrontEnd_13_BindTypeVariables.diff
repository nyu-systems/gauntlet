--- before_pass
+++ after_pass
@@ -6,7 +6,7 @@ parser p(packet_in pk) {
     value_set<tuple<bit<32>, bit<2>>>(4) vs;
     H h;
     state start {
-        pk.extract(h);
+        pk.extract<H>(h);
         transition select(h.f, 2w2) {
             vs: next;
             default: reject;
