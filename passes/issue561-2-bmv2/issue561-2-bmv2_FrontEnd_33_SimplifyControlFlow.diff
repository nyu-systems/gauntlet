--- before_pass
+++ after_pass
@@ -55,10 +55,8 @@ control ingress(inout headers hdr, inout
     apply {
         debug_hdr_0.apply();
         if (hdr.u.short.isValid()) {
-            ;
             hdr.u.short.setInvalid();
         } else if (hdr.u.byte.isValid()) {
-            ;
             hdr.u.byte.setInvalid();
         }
     }
