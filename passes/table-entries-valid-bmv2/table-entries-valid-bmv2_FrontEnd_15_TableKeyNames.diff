--- before_pass
+++ after_pass
@@ -44,8 +44,8 @@ control ingress(inout Header_t h, inout
     }
     table t_valid {
         key = {
-            h.h.isValid(): exact;
-            h.h.e        : exact;
+            h.h.isValid(): exact @name("h.h.$valid$") ;
+            h.h.e        : exact @name("h.h.e") ;
         }
         actions = {
             a();
