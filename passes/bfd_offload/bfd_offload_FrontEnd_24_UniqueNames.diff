--- before_pass
+++ after_pass
@@ -10,11 +10,11 @@ BFD_Offload(16w32768) bfd_session_livene
         this.setTx(index, 8w0);
     }
     bool on_tx(in bit<16> index) {
-        bit<8> c = this.getTx(index) + 8w1;
-        if (c >= 8w4) {
+        bit<8> c_0 = this.getTx(index) + 8w1;
+        if (c_0 >= 8w4) {
             return true;
         } else {
-            this.setTx(index, c);
+            this.setTx(index, c_0);
             return false;
         }
     }
