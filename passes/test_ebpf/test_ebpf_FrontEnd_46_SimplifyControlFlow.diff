--- before_pass
+++ after_pass
@@ -59,9 +59,7 @@ control pipe(inout Headers_t headers, ou
         pass = true;
         if (!headers.ipv4.isValid()) {
             pass = false;
-            {
-                hasReturned = true;
-            }
+            hasReturned = true;
         }
         if (!hasReturned) {
             Check_src_ip_0.apply();
