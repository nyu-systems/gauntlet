--- before_pass
+++ after_pass
@@ -5,10 +5,10 @@ control ctrl() {
     }
     table t {
         actions = {
-            a;
-            b;
+            a();
+            b();
         }
-        default_action = a;
+        default_action = a();
     }
     apply {
         switch (t.apply().action_run) {
