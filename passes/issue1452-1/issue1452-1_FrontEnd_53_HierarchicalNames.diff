--- before_pass
+++ after_pass
@@ -1,6 +1,6 @@
 control c() {
     bit<32> x_0;
-    @name("b") action b(out bit<32> arg) {
+    @name("c.b") action b(out bit<32> arg) {
         arg = 32w2;
     }
     apply {
