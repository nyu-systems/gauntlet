--- before_pass
+++ after_pass
@@ -14,9 +14,9 @@ parser MyP2(packet_in pkt, out headers_t
     }
 }
 parser MyP1(packet_in pkt, out headers_t hdr) {
-    MyP2() subp;
+    @name("subp") MyP2() subp_0;
     state start {
-        subp.apply(pkt, hdr);
+        subp_0.apply(pkt, hdr);
         transition accept;
     }
 }
