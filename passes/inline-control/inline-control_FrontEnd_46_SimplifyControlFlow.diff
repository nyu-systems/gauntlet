--- before_pass
+++ after_pass
@@ -6,10 +6,8 @@ control d(out bit<32> x) {
     @name("cinst.y") Y(32w16) cinst_y;
     bit<32> cinst_tmp;
     apply {
-        {
-            cinst_tmp = cinst_y.get();
-            x = cinst_tmp;
-        }
+        cinst_tmp = cinst_y.get();
+        x = cinst_tmp;
     }
 }
 control dproto(out bit<32> x);
