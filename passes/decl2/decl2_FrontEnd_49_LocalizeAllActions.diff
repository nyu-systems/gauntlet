--- before_pass
+++ after_pass
@@ -3,14 +3,14 @@ control p() {
     bit<1> x;
     bit<1> x_1;
     bit<1> y;
-    @name("b") action b(in bit<1> x_0, out bit<1> y_0) {
+    @name("b") action b_0(in bit<1> x_0, out bit<1> y_0) {
         x = x_0;
         z = x_0 & x;
         y_0 = z;
     }
     apply {
         x_1 = 1w0;
-        b(x_1, y);
+        b_0(x_1, y);
     }
 }
 control simple();
