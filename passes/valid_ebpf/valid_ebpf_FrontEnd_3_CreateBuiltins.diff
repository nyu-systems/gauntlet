--- before_pass
+++ after_pass
@@ -46,9 +46,11 @@ control pipe(inout Headers_t headers, ou
     }
     table t {
         actions = {
-            invalidate;
+            invalidate();
+            @defaultonly NoAction();
         }
         implementation = array_table(1);
+        default_action = NoAction();
     }
     apply {
         if (headers.ipv4.isValid()) {
