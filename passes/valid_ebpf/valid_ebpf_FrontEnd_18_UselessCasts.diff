--- before_pass
+++ after_pass
@@ -54,7 +54,7 @@ control pipe(inout Headers_t headers, ou
     }
     apply {
         if (headers.ipv4.isValid()) {
-            counters.increment((bit<32>)headers.ipv4.dstAddr);
+            counters.increment(headers.ipv4.dstAddr);
             pass = true;
         } else {
             t.apply();
