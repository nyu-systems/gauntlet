--- before_pass
+++ after_pass
@@ -2,10 +2,10 @@ extern void f(in tuple<bit<32>, bool> da
 control proto();
 package top(proto _p);
 control c() {
-    tuple<bit<32>, bool> x;
+    tuple<bit<32>, bool> x_0;
     apply {
-        x = { 32w10, false };
-        f(x);
+        x_0 = { 32w10, false };
+        f(x_0);
         f({ 32w20, true });
     }
 }
