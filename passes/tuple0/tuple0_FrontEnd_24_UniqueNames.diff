--- before_pass
+++ after_pass
@@ -2,9 +2,9 @@ extern void f(in tuple<bit<32>, bool> da
 control proto();
 package top(proto _p);
 control c() {
-    tuple<bit<32>, bool> x = { 32w10, false };
+    tuple<bit<32>, bool> x_0 = { 32w10, false };
     apply {
-        f(x);
+        f(x_0);
         f({ 32w20, true });
     }
 }
