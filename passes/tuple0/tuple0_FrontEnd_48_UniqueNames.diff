--- before_pass
+++ after_pass
@@ -2,10 +2,10 @@ extern void f(in tuple<bit<32>, bool> da
 control proto();
 package top(proto _p);
 control c() {
-    tuple<bit<32>, bool> x_0;
+    tuple<bit<32>, bool> x;
     apply {
-        x_0 = { 32w10, false };
-        f(x_0);
+        x = { 32w10, false };
+        f(x);
         f({ 32w20, true });
     }
 }
