--- before_pass
+++ after_pass
@@ -30,8 +30,8 @@ control MyIC(inout ethernet_t a, inout u
     }
     table tbl {
         key = {
-            a.srcAddr: exact;
-            b.data   : selector;
+            a.srcAddr: exact @name("a.srcAddr") ;
+            b.data   : selector @name("b.data") ;
         }
         actions = {
             NoAction();
