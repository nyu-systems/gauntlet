--- before_pass
+++ after_pass
@@ -37,7 +37,6 @@ control deparser(packet_out b, in Header
     }
 }
 control ingress(inout Header_t h, inout Meta_t m, inout standard_metadata_t standard_meta) {
-    bit<8> error_idx;
     action set_error_idx(bit<8> idx) {
         h.h.e = idx;
     }
