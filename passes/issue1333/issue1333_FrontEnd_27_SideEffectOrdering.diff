--- before_pass
+++ after_pass
@@ -7,8 +7,12 @@ extern E {
 control c()(bit<32> binit=4) {
     @name("e") E(x = 32w0) e_0;
     apply {
-        f(a = 32w0, b = binit);
-        e_0.f(z = 16w2);
+        {
+            f(a = 32w0, b = binit);
+        }
+        {
+            e_0.f(z = 16w2);
+        }
     }
 }
 control ctrl();
