--- before_pass
+++ after_pass
@@ -5,12 +5,12 @@ extern E {
     void f(in bit<16> z=2);
 }
 control c()(bit<32> binit=4) {
-    E() e;
+    E(x = 0) e;
     apply {
-        f(b = binit);
-        e.f();
+        f(a = 0, b = binit);
+        e.f(z = 2);
     }
 }
 control ctrl();
 package top(ctrl _c);
-top(c()) main;
+top(c(binit = 4)) main;
