--- before_pass
+++ after_pass
@@ -5,12 +5,12 @@ extern E {
     void f(in bit<16> z=2);
 }
 control c()(bit<32> binit=4) {
-    E(x = 0) e;
+    E(x = 32w0) e;
     apply {
-        f(a = 0, b = binit);
-        e.f(z = 2);
+        f(a = 32w0, b = binit);
+        e.f(z = 16w2);
     }
 }
 control ctrl();
 package top(ctrl _c);
-top(c(binit = 4)) main;
+top(c(binit = 32w4)) main;
