--- before_pass
+++ after_pass
@@ -4,9 +4,9 @@ control c(inout bit<32> x) {
     }
     table t {
         actions = {
-            a(10);
+            a(32w10);
         }
-        default_action = a(10);
+        default_action = a(32w10);
     }
     apply {
         t.apply();
