--- before_pass
+++ after_pass
@@ -47,10 +47,15 @@ control verifyChecksum(inout headers hdr
     }
 }
 control ingress(inout headers hdr, inout metadata meta, inout standard_metadata_t standard_metadata) {
-    @name("drop") action drop() {
+    @name(".NoAction") action NoAction_1() {
+    }
+    @name("drop") action drop_0() {
+        mark_to_drop(standard_metadata);
+    }
+    @name("drop") action drop_1() {
         mark_to_drop(standard_metadata);
     }
-    @name("ipv4_forward") action ipv4_forward(macAddr_t dstAddr, egressSpec_t port) {
+    @name("ipv4_forward") action ipv4_forward_0(macAddr_t dstAddr, egressSpec_t port) {
         meta.test_bool = true;
     }
     @name("ipv4_lpm") table ipv4_lpm {
@@ -58,12 +63,12 @@ control ingress(inout headers hdr, inout
             hdr.ipv4.dstAddr: lpm @name("hdr.ipv4.dstAddr") ;
         }
         actions = {
-            NoAction();
-            ipv4_forward();
-            drop();
+            NoAction_1();
+            ipv4_forward_0();
+            drop_0();
         }
         size = 1024;
-        default_action = NoAction();
+        default_action = NoAction_1();
     }
     apply {
         meta.test_bool = false;
@@ -71,7 +76,7 @@ control ingress(inout headers hdr, inout
             ipv4_lpm.apply();
         }
         if (!meta.test_bool) {
-            drop();
+            drop_1();
         }
     }
 }
