--- before_pass
+++ after_pass
@@ -20,8 +20,9 @@ control MyC2(in meta_t meta={ 0, 0, 0 })
             meta.f0: exact;
         }
         actions = {
-            NoAction;
+            NoAction();
         }
+        default_action = NoAction();
     }
     apply {
         a.apply();
