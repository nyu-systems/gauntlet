--- before_pass
+++ after_pass
@@ -14,10 +14,10 @@ struct meta_t {
     bit<8> f1;
     bit<8> f2;
 }
-control MyC2(in meta_t meta={ 0, 0, 0 }) {
-    @name("a") table a_0 {
+control MyC(inout hdr_t hdr, inout meta_t meta, in intrinsic_metadata_t intr_md) {
+    @name("c2.a") table c2_a {
         key = {
-            meta.f0: exact @name("meta.f0") ;
+            meta_t {f0 = 8w0,f1 = 8w0,f2 = 8w0}.f0: exact @name("meta.f0") ;
         }
         actions = {
             NoAction();
@@ -25,13 +25,9 @@ control MyC2(in meta_t meta={ 0, 0, 0 })
         default_action = NoAction();
     }
     apply {
-        a_0.apply();
-    }
-}
-control MyC(inout hdr_t hdr, inout meta_t meta, in intrinsic_metadata_t intr_md) {
-    @name("c2") MyC2() c2_0;
-    apply {
-        c2_0.apply(meta = meta_t {f0 = 8w0,f1 = 8w0,f2 = 8w0});
+        {
+            c2_a.apply();
+        }
     }
 }
 P<hdr_t, meta_t>(MyC()) main;
