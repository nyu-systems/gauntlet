--- before_pass
+++ after_pass
@@ -29,13 +29,15 @@ control deparser(packet_out b, in Header
 control ingress(inout Headers h, inout Meta m, inout standard_metadata_t sm) {
     table t1 {
         actions = {
-            NoAction;
+            NoAction();
         }
+        default_action = NoAction();
     }
     table t2 {
         actions = {
-            NoAction;
+            NoAction();
         }
+        default_action = NoAction();
     }
     apply {
         if (m.b == 0) {
