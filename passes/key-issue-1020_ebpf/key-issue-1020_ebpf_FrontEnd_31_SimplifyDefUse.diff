--- before_pass
+++ after_pass
@@ -65,7 +65,7 @@ control pipe(inout Headers_t headers, ou
     }
     apply {
         tmp = t_0.apply().hit;
-        hit_0 = tmp;
+        ;
     }
 }
 ebpfFilter<Headers_t>(prs(), pipe()) main;
