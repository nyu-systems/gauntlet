--- before_pass
+++ after_pass
@@ -40,12 +40,12 @@ parser prs(packet_in p, out Headers_t he
 }
 control pipe(inout Headers_t headers, out bool pass) {
     bool tmp_0;
-    @name("invalidate") action invalidate() {
+    @name("invalidate") action invalidate_0() {
         headers.ipv4.setInvalid();
         headers.ethernet.setInvalid();
         pass = true;
     }
-    @name("drop") action drop() {
+    @name("drop") action drop_0() {
         pass = false;
     }
     @name("t") table t {
@@ -56,11 +56,11 @@ control pipe(inout Headers_t headers, ou
             headers.ethernet.srcAddr   : exact @name("headers.ethernet.srcAddr") ;
         }
         actions = {
-            invalidate();
-            drop();
+            invalidate_0();
+            drop_0();
         }
         implementation = hash_table(32w10);
-        default_action = drop();
+        default_action = drop_0();
     }
     apply {
         tmp_0 = t.apply().hit;
