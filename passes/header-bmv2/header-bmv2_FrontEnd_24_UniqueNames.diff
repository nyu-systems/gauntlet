--- before_pass
+++ after_pass
@@ -5,9 +5,9 @@ header hdr {
 }
 control compute(inout hdr h) {
     apply {
-        hdr tmp;
-        tmp.f = h.f + 32w1;
-        h.f = tmp.f;
+        hdr tmp_0;
+        tmp_0.f = h.f + 32w1;
+        h.f = tmp_0.f;
     }
 }
 struct Headers {
@@ -39,9 +39,9 @@ control deparser(packet_out b, in Header
     }
 }
 control ingress(inout Headers h, inout Meta m, inout standard_metadata_t sm) {
-    compute() c;
+    @name("c") compute() c_0;
     apply {
-        c.apply(h.h);
+        c_0.apply(h.h);
         sm.egress_spec = 9w0;
     }
 }
