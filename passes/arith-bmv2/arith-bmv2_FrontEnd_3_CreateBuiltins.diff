--- before_pass
+++ after_pass
@@ -40,9 +40,9 @@ control ingress(inout Headers h, inout M
     }
     table t {
         actions = {
-            add;
+            add();
         }
-        const default_action = add;
+        const default_action = add();
     }
     apply {
         t.apply();
