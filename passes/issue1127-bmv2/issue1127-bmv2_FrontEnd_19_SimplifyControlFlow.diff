--- before_pass
+++ after_pass
@@ -19,6 +19,7 @@ parser parserI(packet_in pkt, out header
 control cDoOneOp(inout headers hdr, in bit<8> op) {
     apply {
         if (op == 8w0x0) {
+            ;
         } else if (op[7:4] == 4w1) {
             hdr.h1.out1 = 8w4;
         }
