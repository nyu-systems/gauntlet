--- before_pass
+++ after_pass
@@ -1,10 +1,10 @@
 #include <core.p4>
 extern bit<32> f(in bit<32> x);
 parser p() {
-    bit<32> tmp_0;
+    bit<32> tmp;
     state start {
-        tmp_0 = f(32w2);
-        transition select(tmp_0) {
+        tmp = f(32w2);
+        transition select(tmp) {
             32w0: accept;
             default: reject;
         }
