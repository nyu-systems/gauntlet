--- before_pass
+++ after_pass
@@ -1,8 +1,10 @@
 #include <core.p4>
 extern bit<32> f(in bit<32> x);
 parser p() {
+    bit<32> tmp;
     state start {
-        transition select(f(32w2)) {
+        tmp = f(32w2);
+        transition select(tmp) {
             32w0: accept;
             default: reject;
         }
