--- before_pass
+++ after_pass
@@ -31,11 +31,13 @@ control MyIngress(inout my_packet p, ino
     }
     table t {
         actions = {
-            set_data;
+            set_data();
+            @defaultonly NoAction();
         }
         key = {
             meta.err: exact;
         }
+        default_action = NoAction();
     }
     apply {
         t.apply();
