--- before_pass
+++ after_pass
@@ -88,7 +88,7 @@ control cIngress(inout headers hdr, inou
     }
     table guh {
         key = {
-            hdr.tcp.dstPort: exact;
+            hdr.tcp.dstPort: exact @name("hdr.tcp.dstPort") ;
         }
         actions = {
             foo();
