--- before_pass
+++ after_pass
@@ -1,11 +1,9 @@
 #include <core.p4>
 parser p()(bit<1> b, bit<1> c) {
     state start {
-        bit<1> z = b & c;
         transition accept;
     }
 }
-const bit<1> bv = 1w0;
 parser nothing();
 package m(nothing n);
 m(p(1w0, 1w1)) main;
