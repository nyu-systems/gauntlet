--- before_pass
+++ after_pass
@@ -20,8 +20,10 @@ parser MyEP(packet_in buffer, out EMPTY
     }
 }
 control MyIC(inout ethernet_t a, inout EMPTY b, in psa_ingress_input_metadata_t c, inout psa_ingress_output_metadata_t d) {
+    @name(".NoAction") action NoAction_1() {
+    }
     @name("meter0") Meter<bit<12>>(32w1024, PSA_MeterType_t.PACKETS) meter0;
-    @name("execute") action execute_1(bit<12> index, PSA_MeterColor_t color) {
+    @name("execute") action execute_0(bit<12> index, PSA_MeterColor_t color) {
         meter0.execute(index, color);
     }
     @name("tbl") table tbl {
@@ -29,10 +31,10 @@ control MyIC(inout ethernet_t a, inout E
             a.srcAddr: exact @name("a.srcAddr") ;
         }
         actions = {
-            NoAction();
-            execute_1();
+            NoAction_1();
+            execute_0();
         }
-        default_action = NoAction();
+        default_action = NoAction_1();
     }
     apply {
         tbl.apply();
