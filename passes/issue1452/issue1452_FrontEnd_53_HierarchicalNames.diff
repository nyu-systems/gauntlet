--- before_pass
+++ after_pass
@@ -1,6 +1,6 @@
 control c() {
     bit<32> x_0;
-    @name("a") action a(inout bit<32> arg) {
+    @name("c.a") action a(inout bit<32> arg) {
         bool hasReturned = false;
         arg = 32w1;
         hasReturned = true;
