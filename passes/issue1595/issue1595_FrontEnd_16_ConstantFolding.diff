--- before_pass
+++ after_pass
@@ -26,16 +26,16 @@ parser parserI(packet_in pkt, out Parsed
 }
 control cIngress(inout Parsed_packet hdr, inout metadata_t meta, inout standard_metadata_t stdmeta) {
     action a1() {
-        hdr.ethernet.srcAddr = (bit<48>)48w1;
+        hdr.ethernet.srcAddr = 48w1;
     }
     action a2() {
-        hdr.ethernet.srcAddr[47:40] = (bit<8>)8w2;
+        hdr.ethernet.srcAddr[47:40] = 8w2;
     }
     action a3() {
-        hdr.ethernet.srcAddr[47:40] = (bit<8>)8w3;
+        hdr.ethernet.srcAddr[47:40] = 8w3;
     }
     action a4() {
-        hdr.ethernet.srcAddr[47:40] = (bit<8>)8w4;
+        hdr.ethernet.srcAddr[47:40] = 8w4;
     }
     table t1 {
         key = {
@@ -55,16 +55,16 @@ control cIngress(inout Parsed_packet hdr
             a1: {
             }
             a2: {
-                hdr.ethernet.srcAddr[39:32] = (bit<8>)8w2;
+                hdr.ethernet.srcAddr[39:32] = 8w2;
             }
             a3: {
-                hdr.ethernet.srcAddr[39:32] = (bit<8>)8w3;
+                hdr.ethernet.srcAddr[39:32] = 8w3;
             }
             a4: {
-                hdr.ethernet.srcAddr[39:32] = (bit<8>)8w4;
+                hdr.ethernet.srcAddr[39:32] = 8w4;
             }
             NoAction: {
-                hdr.ethernet.srcAddr[39:32] = (bit<8>)8w5;
+                hdr.ethernet.srcAddr[39:32] = 8w5;
             }
         }
     }
