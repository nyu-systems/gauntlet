--- before_pass
+++ after_pass
@@ -12,8 +12,8 @@ parser MyParser(packet_in b, out h hdr,
     state start {
         verify(meta.x == 8s0, error.NewError);
         verify(true, error.NoError);
-        error e = error.NoError;
-        verify(true, e);
+        error e_0 = error.NoError;
+        verify(true, e_0);
         transition accept;
     }
 }
