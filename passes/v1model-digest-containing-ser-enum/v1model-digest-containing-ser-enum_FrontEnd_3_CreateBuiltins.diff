--- before_pass
+++ after_pass
@@ -91,9 +91,9 @@ control MyIngress(inout headers hdr, ino
             hdr.ipv4.dstAddr: exact;
         }
         actions = {
-            set_dmac;
-            drop;
-            NoAction;
+            set_dmac();
+            drop();
+            NoAction();
         }
         size = 1024;
         default_action = NoAction();
@@ -107,9 +107,9 @@ control MyIngress(inout headers hdr, ino
             hdr.ipv4.dstAddr: lpm;
         }
         actions = {
-            set_nhop;
-            drop;
-            NoAction;
+            set_nhop();
+            drop();
+            NoAction();
         }
         size = 1024;
         default_action = NoAction();
@@ -141,8 +141,8 @@ control MyEgress(inout headers hdr, inou
             standard_metadata.egress_port: exact;
         }
         actions = {
-            rewrite_mac;
-            NoAction;
+            rewrite_mac();
+            NoAction();
         }
         size = 1024;
         default_action = NoAction();
