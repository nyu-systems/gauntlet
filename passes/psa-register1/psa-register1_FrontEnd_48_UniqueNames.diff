--- before_pass
+++ after_pass
@@ -20,23 +20,23 @@ parser MyEP(packet_in buffer, out EMPTY
     }
 }
 control MyIC(inout ethernet_t a, inout EMPTY b, in psa_ingress_input_metadata_t c, inout psa_ingress_output_metadata_t d) {
-    bit<10> tmp;
-    @name("reg") Register<bit<10>, bit<10>>(32w1024) reg_0;
-    @name("execute_register") action execute_register_0(bit<10> idx) {
-        tmp = reg_0.read(idx);
+    bit<10> tmp_0;
+    @name("reg") Register<bit<10>, bit<10>>(32w1024) reg;
+    @name("execute_register") action execute_register(bit<10> idx) {
+        tmp_0 = reg.read(idx);
     }
-    @name("tbl") table tbl_0 {
+    @name("tbl") table tbl {
         key = {
             a.srcAddr: exact @name("a.srcAddr") ;
         }
         actions = {
             NoAction();
-            execute_register_0();
+            execute_register();
         }
         default_action = NoAction();
     }
     apply {
-        tbl_0.apply();
+        tbl.apply();
     }
 }
 control MyEC(inout EMPTY a, inout EMPTY b, in psa_egress_input_metadata_t c, inout psa_egress_output_metadata_t d) {
