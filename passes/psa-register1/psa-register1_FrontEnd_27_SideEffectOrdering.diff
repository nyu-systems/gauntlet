--- before_pass
+++ after_pass
@@ -10,7 +10,9 @@ header ethernet_t {
 }
 parser MyIP(packet_in buffer, out ethernet_t eth, inout EMPTY b, in psa_ingress_parser_input_metadata_t c, in EMPTY d, in EMPTY e) {
     state start {
-        buffer.extract<ethernet_t>(eth);
+        {
+            buffer.extract<ethernet_t>(eth);
+        }
         transition accept;
     }
 }
@@ -23,7 +25,11 @@ control MyIC(inout ethernet_t a, inout E
     bit<10> data_0;
     @name("reg") Register<bit<10>, bit<10>>(32w1024) reg_0;
     @name("execute_register") action execute_register_0(bit<10> idx) {
-        data_0 = reg_0.read(idx);
+        bit<10> tmp;
+        {
+            tmp = reg_0.read(idx);
+            data_0 = tmp;
+        }
     }
     @name("tbl") table tbl_0 {
         key = {
@@ -36,7 +42,9 @@ control MyIC(inout ethernet_t a, inout E
         default_action = NoAction();
     }
     apply {
-        tbl_0.apply();
+        {
+            tbl_0.apply();
+        }
     }
 }
 control MyEC(inout EMPTY a, inout EMPTY b, in psa_egress_input_metadata_t c, inout psa_egress_output_metadata_t d) {
