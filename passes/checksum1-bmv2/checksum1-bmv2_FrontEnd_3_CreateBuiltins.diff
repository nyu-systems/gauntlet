--- before_pass
+++ after_pass
@@ -90,9 +90,9 @@ control cIngress(inout headers hdr, inou
             hdr.tcp.dstPort: exact;
         }
         actions = {
-            foo;
+            foo();
         }
-        default_action = foo;
+        default_action = foo();
     }
     apply {
         guh.apply();
