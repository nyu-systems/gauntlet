--- before_pass
+++ after_pass
@@ -42,7 +42,7 @@ parser CommonParser(packet_in buffer, ou
     state parse_ethernet {
         buffer.extract(parsed_hdr.ethernet);
         transition select(parsed_hdr.ethernet.etherType) {
-            0x800: parse_ipv4;
+            16w0x800: parse_ipv4;
             default: accept;
         }
     }
