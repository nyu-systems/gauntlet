--- before_pass
+++ after_pass
@@ -4,7 +4,7 @@ extern Fake {
     void call(in bit<32> data);
 }
 parser P() {
-    bit<32> x = (bit<32>)32w0;
+    bit<32> x = 32w0;
     Fake() fake;
     state start {
         fake.call(x);
@@ -12,7 +12,7 @@ parser P() {
     }
 }
 control C() {
-    bit<32> x = (bit<32>)32w0;
+    bit<32> x = 32w0;
     bit<32> y = x + 32w1;
     Fake() fake;
     apply {
