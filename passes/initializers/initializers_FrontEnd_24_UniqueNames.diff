--- before_pass
+++ after_pass
@@ -4,19 +4,19 @@ extern Fake {
     void call(in bit<32> data);
 }
 parser P() {
-    bit<32> x = 32w0;
-    Fake() fake;
+    bit<32> x_0 = 32w0;
+    @name("fake") Fake() fake_0;
     state start {
-        fake.call(x);
+        fake_0.call(x_0);
         transition accept;
     }
 }
 control C() {
-    bit<32> x = 32w0;
-    bit<32> y = x + 32w1;
-    Fake() fake;
+    bit<32> x_1 = 32w0;
+    bit<32> y_0 = x_1 + 32w1;
+    @name("fake") Fake() fake_1;
     apply {
-        fake.call(y);
+        fake_1.call(y_0);
     }
 }
 parser SimpleParser();
