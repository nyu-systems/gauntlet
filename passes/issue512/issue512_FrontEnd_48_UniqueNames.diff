--- before_pass
+++ after_pass
@@ -25,28 +25,28 @@ parser parserI(packet_in pkt, out Parsed
     }
 }
 control cIngress(inout Parsed_packet hdr, inout mystruct1 meta, inout standard_metadata_t stdmeta) {
-    @name("foo") action foo_0() {
-        bool hasReturned = false;
+    @name("foo") action foo() {
+        bool hasReturned_0 = false;
         meta.b = meta.b + 4w5;
         if (meta.b > 4w10) {
             meta.b = meta.b ^ 4w5;
-            hasReturned = true;
+            hasReturned_0 = true;
         }
-        if (!hasReturned) {
+        if (!hasReturned_0) {
             meta.b = meta.b + 4w5;
         }
     }
-    @name("guh") table guh_0 {
+    @name("guh") table guh {
         key = {
             hdr.ethernet.srcAddr: exact @name("hdr.ethernet.srcAddr") ;
         }
         actions = {
-            foo_0();
+            foo();
         }
-        default_action = foo_0();
+        default_action = foo();
     }
     apply {
-        guh_0.apply();
+        guh.apply();
     }
 }
 control cEgress(inout Parsed_packet hdr, inout mystruct1 meta, inout standard_metadata_t stdmeta) {
