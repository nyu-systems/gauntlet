--- before_pass
+++ after_pass
@@ -26,12 +26,17 @@ parser parserI(packet_in pkt, out Parsed
 }
 control cIngress(inout Parsed_packet hdr, inout mystruct1 meta, inout standard_metadata_t stdmeta) {
     @name("foo") action foo_0() {
+        bool hasReturned = false;
         meta.b = meta.b + 4w5;
         if (meta.b > 4w10) {
             meta.b = meta.b ^ 4w5;
-            return;
+            {
+                hasReturned = true;
+            }
+        }
+        if (!hasReturned) {
+            meta.b = meta.b + 4w5;
         }
-        meta.b = meta.b + 4w5;
     }
     @name("guh") table guh_0 {
         key = {
