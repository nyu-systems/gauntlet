--- before_pass
+++ after_pass
@@ -38,9 +38,9 @@ control cIngress(inout Parsed_packet hdr
             hdr.ethernet.srcAddr: exact;
         }
         actions = {
-            foo;
+            foo();
         }
-        default_action = foo;
+        default_action = foo();
     }
     apply {
         guh.apply();
