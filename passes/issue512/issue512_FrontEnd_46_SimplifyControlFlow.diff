--- before_pass
+++ after_pass
@@ -30,9 +30,7 @@ control cIngress(inout Parsed_packet hdr
         meta.b = meta.b + 4w5;
         if (meta.b > 4w10) {
             meta.b = meta.b ^ 4w5;
-            {
-                hasReturned = true;
-            }
+            hasReturned = true;
         }
         if (!hasReturned) {
             meta.b = meta.b + 4w5;
