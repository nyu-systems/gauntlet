--- before_pass
+++ after_pass
@@ -10,6 +10,7 @@ parser ParserI(packet_in pk, out H hdr,
     }
 }
 control IngressI(inout H hdr, inout M meta, inout standard_metadata_t smeta) {
+    bool tmp;
     @name("t") table t_0 {
         key = {
         }
@@ -18,7 +19,6 @@ control IngressI(inout H hdr, inout M me
         }
         default_action = NoAction();
     }
-    bool tmp;
     apply {
         tmp = t_0.apply().hit;
     }
