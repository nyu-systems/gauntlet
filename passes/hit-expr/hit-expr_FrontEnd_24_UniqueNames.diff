--- before_pass
+++ after_pass
@@ -10,7 +10,7 @@ parser ParserI(packet_in pk, out H hdr,
     }
 }
 control IngressI(inout H hdr, inout M meta, inout standard_metadata_t smeta) {
-    table t {
+    @name("t") table t_0 {
         key = {
         }
         actions = {
@@ -19,7 +19,7 @@ control IngressI(inout H hdr, inout M me
         default_action = NoAction();
     }
     apply {
-        if (!t.apply().hit) {
+        if (!t_0.apply().hit) {
             ;
         }
     }
