--- before_pass
+++ after_pass
@@ -167,8 +167,8 @@ parser ParserImpl(packet_in packet, out
         transition accept;
     }
 }
-action my_drop(inout standard_metadata_t smeta) {
-    mark_to_drop(smeta);
+action my_drop(inout standard_metadata_t smeta_0) {
+    mark_to_drop(smeta_0);
 }
 control ingress(inout headers hdr, inout metadata meta, inout standard_metadata_t standard_metadata) {
     @name("set_l2ptr") action set_l2ptr_0(bit<32> l2ptr) {
