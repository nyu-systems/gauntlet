--- before_pass
+++ after_pass
@@ -180,7 +180,7 @@ control ingress(inout headers hdr, inout
             hdr.ipv4.dstAddr: lpm;
         }
         actions = {
-            set_l2ptr;
+            set_l2ptr();
             my_drop(standard_metadata);
         }
         default_action = my_drop(standard_metadata);
@@ -196,7 +196,7 @@ control ingress(inout headers hdr, inout
             meta.fwd_metadata.l2ptr: exact;
         }
         actions = {
-            set_bd_dmac_intf;
+            set_bd_dmac_intf();
             my_drop(standard_metadata);
         }
         default_action = my_drop(standard_metadata);
@@ -215,7 +215,7 @@ control egress(inout headers hdr, inout
             meta.fwd_metadata.out_bd: exact;
         }
         actions = {
-            rewrite_mac;
+            rewrite_mac();
             my_drop(standard_metadata);
         }
         default_action = my_drop(standard_metadata);
