--- before_pass
+++ after_pass
@@ -61,7 +61,7 @@ control cIngress(inout Parsed_packet hdr
     }
     table guh {
         key = {
-            hdr.ethernet.srcAddr: exact;
+            hdr.ethernet.srcAddr: exact @name("hdr.ethernet.srcAddr") ;
         }
         actions = {
             foo();
