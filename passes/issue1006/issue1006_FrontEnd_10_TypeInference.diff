--- before_pass
+++ after_pass
@@ -7,8 +7,8 @@ struct foo {
 control c();
 package top(c _c);
 control c1() {
-    R<tuple<bit<8>>>({ 1 }) reg0;
-    R<foo>({ 1 }) reg1;
+    R<tuple<bit<8>>>({ 8w1 }) reg0;
+    R<foo>({ 8w1 }) reg1;
     apply {
     }
 }
