--- before_pass
+++ after_pass
@@ -11,11 +11,17 @@ control c(inout bit<16> p) {
     @name("cntr") Virtual() cntr_0 = {
         @name("state") State(16s1024) state_0;
         bit<16> f(in bit<16> ix) {
-            return state_0.get(ix);
+            bit<16> tmp;
+            {
+                tmp = state_0.get(ix);
+                return tmp;
+            }
         }
     };
     apply {
-        cntr_0.run(16w6);
+        {
+            cntr_0.run(16w6);
+        }
     }
 }
 control ctr(inout bit<16> x);
