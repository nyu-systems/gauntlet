--- before_pass
+++ after_pass
@@ -4,9 +4,11 @@ parser p() {
     }
 }
 parser q() {
+    @name("p") p() p_inst;
+    @name("p") p() p_inst_0;
     state start {
-        p.apply();
-        p.apply();
+        p_inst.apply();
+        p_inst_0.apply();
         transition accept;
     }
 }
