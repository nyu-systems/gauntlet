--- before_pass
+++ after_pass
@@ -24,8 +24,10 @@ control MyIngress(inout headers hdr, ino
     }
     table t {
         actions = {
-            a;
+            a();
+            @defaultonly NoAction();
         }
+        default_action = NoAction();
     }
     apply {
         t.apply();
