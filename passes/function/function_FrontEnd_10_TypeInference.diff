--- before_pass
+++ after_pass
@@ -6,7 +6,7 @@ bit<16> max(in bit<16> left, in bit<16>
 }
 control c(out bit<16> b) {
     apply {
-        b = max(10, 12);
+        b = max(16w10, 16w12);
     }
 }
 control ctr(out bit<16> b);
