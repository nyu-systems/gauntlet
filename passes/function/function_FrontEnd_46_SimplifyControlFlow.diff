--- before_pass
+++ after_pass
@@ -11,10 +11,8 @@ control c(out bit<16> b) {
                 retval = left_0;
             }
             if (!hasReturned) {
-                {
-                    hasReturned = true;
-                    retval = right_0;
-                }
+                hasReturned = true;
+                retval = right_0;
             }
             tmp = retval;
         }
