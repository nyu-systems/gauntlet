--- before_pass
+++ after_pass
@@ -7,10 +7,8 @@ bit<16> max(in bit<16> left, in bit<16>
 control c(out bit<16> b) {
     bit<16> tmp;
     apply {
-        {
-            tmp = max(16w10, 16w12);
-            b = tmp;
-        }
+        tmp = max(16w10, 16w12);
+        b = tmp;
     }
 }
 control ctr(out bit<16> b);
