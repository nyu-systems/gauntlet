--- before_pass
+++ after_pass
@@ -6,7 +6,6 @@ control proto();
 package top(proto _p);
 control c() {
     tuple<bit<32>, bool> x_0;
-    tuple<bit<32>, bool> y_0;
     apply {
         x_0 = { 32w10, false };
     }
