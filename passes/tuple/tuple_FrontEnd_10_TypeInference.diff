--- before_pass
+++ after_pass
@@ -6,7 +6,7 @@ struct S {
 control proto();
 package top(proto _p);
 control c() {
-    pair x = { 10, false };
+    tuple<bit<32>, bool> x = { 32w10, false };
     tuple<bit<32>, bool> y;
     apply {
         y = x;
