--- before_pass
+++ after_pass
@@ -5,9 +5,10 @@ struct S {
 control proto();
 package top(proto _p);
 control c() {
-    tuple<bit<32>, bool> x_0 = { 32w10, false };
+    tuple<bit<32>, bool> x_0;
     tuple<bit<32>, bool> y_0;
     apply {
+        x_0 = { 32w10, false };
         y_0 = x_0;
     }
 }
