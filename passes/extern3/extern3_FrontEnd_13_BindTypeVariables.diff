--- before_pass
+++ after_pass
@@ -6,6 +6,6 @@ extern ext<H, V> {
 control c() {
     ext<bit<16>, void>(16w0) x;
     apply {
-        x.method1(x.method2(12w1), 8w0);
+        x.method1<bit<8>>(x.method2<bit<12>>(12w1), 8w0);
     }
 }
