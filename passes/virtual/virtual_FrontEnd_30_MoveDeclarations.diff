--- before_pass
+++ after_pass
@@ -8,6 +8,7 @@ extern Virtual {
     abstract void g(inout data ix);
 }
 control c(inout bit<16> p) {
+    bit<16> tmp;
     @name("cntr") Virtual() cntr_0 = {
         bit<16> f(in bit<16> ix) {
             return ix + 16w1;
@@ -23,7 +24,6 @@ control c(inout bit<16> p) {
             }
         }
     };
-    bit<16> tmp;
     apply {
         tmp = cntr_0.f(16w6);
         p = tmp;
