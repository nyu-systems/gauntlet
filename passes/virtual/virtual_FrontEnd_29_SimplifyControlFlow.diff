--- before_pass
+++ after_pass
@@ -14,9 +14,7 @@ control c(inout bit<16> p) {
         }
         void g(inout data x) {
             data ix_0;
-            {
-                ix_0 = x;
-            }
+            ix_0 = x;
             if (ix_0.a < ix_0.b) {
                 x.a = ix_0.a + 16w1;
             }
@@ -27,10 +25,8 @@ control c(inout bit<16> p) {
     };
     bit<16> tmp;
     apply {
-        {
-            tmp = cntr_0.f(16w6);
-            p = tmp;
-        }
+        tmp = cntr_0.f(16w6);
+        p = tmp;
     }
 }
 control ctr(inout bit<16> x);
