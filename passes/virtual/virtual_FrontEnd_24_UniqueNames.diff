--- before_pass
+++ after_pass
@@ -8,22 +8,22 @@ extern Virtual {
     abstract void g(inout data ix);
 }
 control c(inout bit<16> p) {
-    Virtual() cntr = {
+    @name("cntr") Virtual() cntr_0 = {
         bit<16> f(in bit<16> ix) {
             return ix + 16w1;
         }
         void g(inout data x) {
-            data ix = x;
-            if (ix.a < ix.b) {
-                x.a = ix.a + 16w1;
+            data ix_0 = x;
+            if (ix_0.a < ix_0.b) {
+                x.a = ix_0.a + 16w1;
             }
-            if (ix.a > ix.b) {
-                x.a = ix.a + 16w65535;
+            if (ix_0.a > ix_0.b) {
+                x.a = ix_0.a + 16w65535;
             }
         }
     };
     apply {
-        p = cntr.f(16w6);
+        p = cntr_0.f(16w6);
     }
 }
 control ctr(inout bit<16> x);
