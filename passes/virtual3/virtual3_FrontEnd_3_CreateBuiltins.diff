--- before_pass
+++ after_pass
@@ -23,9 +23,11 @@ control c(inout bit<16> p) {
             p: exact;
         }
         actions = {
-            add_ctr;
-            final_ctr;
+            add_ctr();
+            final_ctr();
+            @defaultonly NoAction();
         }
+        default_action = NoAction();
     }
     apply {
         local = 4;
