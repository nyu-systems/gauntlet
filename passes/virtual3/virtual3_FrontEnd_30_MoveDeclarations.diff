--- before_pass
+++ after_pass
@@ -7,13 +7,13 @@ extern Virtual {
 }
 control c(inout bit<16> p) {
     bit<16> local_0;
+    bit<16> tmp;
     @name("cntr") Virtual() cntr_0 = {
         bit<16> f(in bit<16> ix) {
             return ix + local_0;
         }
     };
     @name("final_ctr") action final_ctr_0() {
-        bit<16> tmp;
         tmp = cntr_0.total();
         p = tmp;
     }
