--- before_pass
+++ after_pass
@@ -1,18 +1,14 @@
 #include <core.p4>
 parser p1(out bit<2> w)(bit<2> a) {
     state start {
-        {
-            w = 2w2;
-        }
+        w = 2w2;
         transition accept;
     }
 }
 parser p2(out bit<2> w)(bit<2> a) {
     @name("x") p1(a) x_0;
     state start {
-        {
-            x_0.apply(w);
-        }
+        x_0.apply(w);
         transition accept;
     }
 }
