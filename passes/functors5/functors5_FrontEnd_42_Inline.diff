--- before_pass
+++ after_pass
@@ -1,16 +1,15 @@
 #include <core.p4>
 parser simple(out bit<2> w);
 package m(simple n);
-parser p1_0(out bit<2> w) {
+parser p2_0(out bit<2> w) {
     state start {
+        transition p1_0_start;
+    }
+    state p1_0_start {
         w = 2w2;
-        transition accept;
+        transition start_0;
     }
-}
-parser p2_0(out bit<2> w) {
-    @name("x") p1_0() x_0;
-    state start {
-        x_0.apply(w);
+    state start_0 {
         transition accept;
     }
 }
