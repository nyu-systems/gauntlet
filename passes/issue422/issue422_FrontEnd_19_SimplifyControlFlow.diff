--- before_pass
+++ after_pass
@@ -24,9 +24,7 @@ parser parserI(packet_in pkt, out Parsed
     state start {
         const bit<16> c1 = 16w0xdead;
         bit<16> v1;
-        {
-            pkt.extract<Ethernet_h>(hdr.ethernet);
-        }
+        pkt.extract<Ethernet_h>(hdr.ethernet);
         transition accept;
     }
 }
