--- before_pass
+++ after_pass
@@ -37,10 +37,10 @@ control cIngress(inout Parsed_packet hdr
         key = {
         }
         actions = {
-            foo;
-            NoAction;
+            foo();
+            NoAction();
         }
-        default_action = NoAction;
+        default_action = NoAction();
     }
     apply {
         tbl1.apply();
