--- before_pass
+++ after_pass
@@ -33,10 +33,10 @@ control deparser(packet_out b, in Header
     }
 }
 control ingress(inout Headers h, inout Meta m, inout standard_metadata_t sm) {
-    @name("c.add") action c_add_0(bit<32> data) {
+    @name("ingress.c.add") action c_add_0(bit<32> data) {
         h.h.b = h.h.a + data;
     }
-    @name("c.t") table c_t {
+    @name("ingress.c.t") table c_t {
         actions = {
             c_add_0();
         }
