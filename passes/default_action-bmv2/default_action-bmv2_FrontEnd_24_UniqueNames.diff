--- before_pass
+++ after_pass
@@ -5,17 +5,17 @@ header hdr {
     bit<32> b;
 }
 control compute(inout hdr h) {
-    action add(bit<32> data) {
+    @name("add") action add_0(bit<32> data) {
         h.b = h.a + data;
     }
-    table t {
+    @name("t") table t_0 {
         actions = {
-            add();
+            add_0();
         }
-        const default_action = add(32w10);
+        const default_action = add_0(32w10);
     }
     apply {
-        t.apply();
+        t_0.apply();
     }
 }
 struct Headers {
@@ -47,9 +47,9 @@ control deparser(packet_out b, in Header
     }
 }
 control ingress(inout Headers h, inout Meta m, inout standard_metadata_t sm) {
-    compute() c;
+    @name("c") compute() c_0;
     apply {
-        c.apply(h.h);
+        c_0.apply(h.h);
         sm.egress_spec = 9w0;
     }
 }
