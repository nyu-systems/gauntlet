--- before_pass
+++ after_pass
@@ -12,7 +12,7 @@ control compute(inout hdr h) {
         actions = {
             add();
         }
-        const default_action = add(10);
+        const default_action = add(32w10);
     }
     apply {
         t.apply();
@@ -50,7 +50,7 @@ control ingress(inout Headers h, inout M
     compute() c;
     apply {
         c.apply(h.h);
-        sm.egress_spec = 0;
+        sm.egress_spec = (bit<9>)9w0;
     }
 }
 V1Switch(p(), vrfy(), ingress(), egress(), update(), deparser()) main;
