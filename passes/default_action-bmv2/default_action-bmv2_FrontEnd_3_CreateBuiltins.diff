--- before_pass
+++ after_pass
@@ -10,7 +10,7 @@ control compute(inout hdr h) {
     }
     table t {
         actions = {
-            add;
+            add();
         }
         const default_action = add(10);
     }
