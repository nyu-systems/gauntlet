--- before_pass
+++ after_pass
@@ -4,12 +4,8 @@ package top(proto _p);
 control c() {
     tuple<bit<32>, bool> x;
     apply {
-        {
-            x = { 32w10, false };
-        }
-        {
-            f<tuple<bit<32>, bool>>(x);
-        }
+        x = { 32w10, false };
+        f<tuple<bit<32>, bool>>(x);
     }
 }
 top(c()) main;
