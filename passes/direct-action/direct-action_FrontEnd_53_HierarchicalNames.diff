--- before_pass
+++ after_pass
@@ -1,6 +1,6 @@
 control c(inout bit<16> y) {
     bit<32> x_0;
-    @name("a") action a(in bit<32> arg) {
+    @name("c.a") action a(in bit<32> arg) {
         y = (bit<16>)arg;
     }
     apply {
