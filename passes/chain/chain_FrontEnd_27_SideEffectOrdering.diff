--- before_pass
+++ after_pass
@@ -4,7 +4,9 @@ header Header {
 }
 parser p1(packet_in p, out Header h) {
     state start {
-        p.extract<Header>(h);
+        {
+            p.extract<Header>(h);
+        }
         transition accept;
     }
 }
