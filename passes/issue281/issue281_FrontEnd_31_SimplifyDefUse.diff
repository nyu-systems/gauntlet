--- before_pass
+++ after_pass
@@ -75,7 +75,7 @@ parser L3(packet_in b, inout h hdr) {
     }
     state vlan {
         vp_0.apply(b, hdr.vlan);
-        etherType_0 = hdr.vlan.etherType;
+        ;
         transition start;
     }
 }
