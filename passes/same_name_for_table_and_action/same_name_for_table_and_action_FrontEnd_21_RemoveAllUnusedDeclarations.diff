--- before_pass
+++ after_pass
@@ -10,8 +10,6 @@ parser ParserI(packet_in pk, out H hdr,
         transition accept;
     }
 }
-action empty() {
-}
 control IngressI(inout H hdr, inout M meta, inout standard_metadata_t smeta) {
     @name("do_something") action do_something_0() {
         mark_to_drop(smeta);
