--- before_pass
+++ after_pass
@@ -46,7 +46,7 @@ control pipe(inout Headers_t headers, ou
     }
     table Check_src_ip {
         key = {
-            headers.ipv4[0].srcAddr: exact;
+            headers.ipv4[0].srcAddr: exact @name("headers.ipv4[0].srcAddr") ;
         }
         actions = {
             Reject();
