--- before_pass
+++ after_pass
@@ -40,7 +40,9 @@ parser prs(packet_in p, out Headers_t he
     }
 }
 control pipe(inout Headers_t headers, out bool pass) {
-    @name("Reject") action Reject(IPv4Address add) {
+    @name(".NoAction") action NoAction_1() {
+    }
+    @name("Reject") action Reject_0(IPv4Address add) {
         pass = false;
         headers.ipv4[0].srcAddr = add;
     }
@@ -49,19 +51,19 @@ control pipe(inout Headers_t headers, ou
             headers.ipv4[0].srcAddr: exact @name("headers.ipv4[0].srcAddr") ;
         }
         actions = {
-            Reject();
-            NoAction();
+            Reject_0();
+            NoAction_1();
         }
         implementation = hash_table(32w1024);
-        const default_action = NoAction();
+        const default_action = NoAction_1();
     }
     apply {
         pass = true;
         switch (Check_src_ip.apply().action_run) {
-            Reject: {
+            Reject_0: {
                 pass = false;
             }
-            NoAction: {
+            NoAction_1: {
             }
         }
     }
