--- before_pass
+++ after_pass
@@ -59,7 +59,7 @@ control ingress(inout headers_t hdr, ino
             hdr.ipv4.dstAddr: lpm;
         }
         actions = {
-            set_output;
+            set_output();
             my_drop(standard_metadata);
         }
         default_action = my_drop(standard_metadata);
@@ -70,9 +70,9 @@ control ingress(inout headers_t hdr, ino
         }
         actions = {
             my_drop(standard_metadata);
-            NoAction;
+            NoAction();
         }
-        const default_action = NoAction;
+        const default_action = NoAction();
     }
     apply {
         if (hdr.ipv4.isValid()) {
