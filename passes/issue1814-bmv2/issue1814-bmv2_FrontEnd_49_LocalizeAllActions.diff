--- before_pass
+++ after_pass
@@ -11,6 +11,8 @@ parser ParserImpl(packet_in packet, out
     }
 }
 control IngressImpl(inout headers hdr, inout metadata meta, inout standard_metadata_t standard_metadata) {
+    @name(".NoAction") action NoAction_1() {
+    }
     bit<1> registerData;
     @name("testRegister") register<bit<1>>(32w1) testRegister;
     @name("debug_table") table debug_table {
@@ -18,9 +20,9 @@ control IngressImpl(inout headers hdr, i
             meta.test: exact @name("meta.test") ;
         }
         actions = {
-            NoAction();
+            NoAction_1();
         }
-        default_action = NoAction();
+        default_action = NoAction_1();
     }
     apply {
         testRegister.read(registerData, 32w0);
