--- before_pass
+++ after_pass
@@ -22,7 +22,7 @@ parser ParserImpl(packet_in packet, out
 }
 control bar() {
     apply {
-        const foo_t FOO = (foo_t)1;
-        foo_t tmp = (foo_t)1;
+        const foo_t FOO = (foo_t)(bit<7>)7w1;
+        foo_t tmp = (foo_t)(bit<7>)7w1;
     }
 }
