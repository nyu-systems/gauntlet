--- before_pass
+++ after_pass
@@ -23,6 +23,6 @@ parser ParserImpl(packet_in packet, out
 control bar() {
     apply {
         const foo_t FOO = (foo_t)1;
-        foo_t tmp = FOO;
+        foo_t tmp = (foo_t)1;
     }
 }
