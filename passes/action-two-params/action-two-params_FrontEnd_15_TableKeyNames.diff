--- before_pass
+++ after_pass
@@ -40,7 +40,7 @@ control MyIngress(inout headers hdr, ino
     }
     table ingress_tbl {
         key = {
-            hdr.ipv4.dstAddr: exact;
+            hdr.ipv4.dstAddr: exact @name("hdr.ipv4.dstAddr") ;
         }
         actions = {
             actTbl();
