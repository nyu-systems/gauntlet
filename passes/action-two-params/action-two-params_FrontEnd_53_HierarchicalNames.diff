--- before_pass
+++ after_pass
@@ -33,12 +33,12 @@ parser MyParser(packet_in packet, out he
     }
 }
 control MyIngress(inout headers hdr, inout metadata meta, inout standard_metadata_t standard_metadata) {
-    @name("drop") action drop() {
+    @name("MyIngress.drop") action drop() {
         mark_to_drop(standard_metadata);
     }
-    @name("actTbl") action actTbl(bit<24> id, bit<32> ip) {
+    @name("MyIngress.actTbl") action actTbl(bit<24> id, bit<32> ip) {
     }
-    @name("ingress_tbl") table ingress_tbl_0 {
+    @name("MyIngress.ingress_tbl") table ingress_tbl_0 {
         key = {
             hdr.ipv4.dstAddr: exact @name("hdr.ipv4.dstAddr") ;
         }
