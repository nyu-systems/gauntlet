--- before_pass
+++ after_pass
@@ -6,11 +6,11 @@ parser adder(in bit<32> y, out bit<32> x
     }
 }
 parser par(out bool b) {
-    adder(ignore = false, add = 6) p;
+    adder(ignore = false, add = 32w6) p;
     state start {
         bit<32> x;
-        p.apply(x = x, y = 0);
-        b = x == 0;
+        p.apply(x = x, y = 32w0);
+        b = x == 32w0;
         transition accept;
     }
 }
@@ -20,18 +20,18 @@ control comp(inout bit<16> x, out bool b
     }
 }
 control c(out bool b) {
-    comp(ignore = 1, compare = 0) c0;
-    comp(ignore = 2, compare = 1) c1;
+    comp(ignore = 2w1, compare = 16w0) c0;
+    comp(ignore = 2w2, compare = 16w1) c1;
     action a(in bit<16> bi, out bit<16> mb) {
         mb = -bi;
     }
     apply {
-        bit<16> xv = 0;
-        a(bi = 3, mb = xv);
-        a(mb = xv, bi = 0);
+        bit<16> xv = (bit<16>)16w0;
+        a(bi = 16w3, mb = xv);
+        a(mb = xv, bi = 16w0);
         c0.apply(b = b, x = xv);
         c1.apply(xv, b);
-        xv = 1;
+        xv = (bit<16>)16w1;
         c0.apply(x = xv, b = b);
         c1.apply(b = b, x = xv);
     }
