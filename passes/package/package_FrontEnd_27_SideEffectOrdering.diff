--- before_pass
+++ after_pass
@@ -2,7 +2,9 @@ control proto(out bit<32> o);
 package top(proto _c, bool parameter);
 control c(out bit<32> o) {
     apply {
-        o = 32w0;
+        {
+            o = 32w0;
+        }
     }
 }
 top(c(), true) main;
