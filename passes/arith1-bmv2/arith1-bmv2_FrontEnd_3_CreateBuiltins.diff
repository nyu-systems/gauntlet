--- before_pass
+++ after_pass
@@ -40,9 +40,9 @@ control ingress(inout Headers h, inout M
     }
     table t {
         actions = {
-            compare;
+            compare();
         }
-        const default_action = compare;
+        const default_action = compare();
     }
     apply {
         t.apply();
