--- before_pass
+++ after_pass
@@ -14,8 +14,11 @@ extern ValueSet {
 parser TopParser(packet_in b, out Parsed_packet p) {
     bit<8> setIndex_0;
     @name("ethtype_kinds") ValueSet(32w5) ethtype_kinds_0;
+    bit<8> tmp;
     state start {
-        b.extract<Ethernet_h>(p.ethernet);
+        {
+            b.extract<Ethernet_h>(p.ethernet);
+        }
         transition select(p.ethernet.etherType) {
             16w0x800: parse_ipv4;
             16w0x806: parse_arp;
@@ -24,7 +27,10 @@ parser TopParser(packet_in b, out Parsed
         }
     }
     state dispatch_value_sets {
-        setIndex_0 = ethtype_kinds_0.index(p.ethernet.etherType);
+        {
+            tmp = ethtype_kinds_0.index(p.ethernet.etherType);
+            setIndex_0 = tmp;
+        }
         transition select(setIndex_0) {
             8w1: parse_trill;
             8w2: parse_vlan_tag;
