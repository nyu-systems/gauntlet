--- before_pass
+++ after_pass
@@ -23,6 +23,7 @@ parser p(packet_in b, out headers hdr, i
     }
 }
 control ingress(inout headers hdr, inout metadata meta, inout standard_metadata_t stdmeta) {
+    H[2] tmp_0;
     apply {
         hdr.same.setValid();
         hdr.same.same = 8w0;
@@ -36,7 +37,6 @@ control ingress(inout headers hdr, inout
         if (hdr.h == hdr.a[0]) {
             hdr.same.same = hdr.same.same | 8w4;
         }
-        H[2] tmp_0;
         tmp_0[0] = hdr.h;
         tmp_0[1] = hdr.a[0];
         if (tmp_0 == hdr.a) {
