--- before_pass
+++ after_pass
@@ -13,7 +13,6 @@ parser p(out bit<1> z)(bit<1> b, bit<1>
         transition accept;
     }
 }
-const bit<1> bv = 1w0;
 parser simple(out bit<1> z);
 package m(simple n);
 m(p(1w0, 1w1)) main;
