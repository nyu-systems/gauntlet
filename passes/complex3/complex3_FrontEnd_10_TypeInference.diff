--- before_pass
+++ after_pass
@@ -1,7 +1,7 @@
 extern bit<32> f(in bit<32> x);
 control c(inout bit<32> r) {
     apply {
-        r = f(4) + f(5);
+        r = f(32w4) + f(32w5);
     }
 }
 control simple(inout bit<32> r);
