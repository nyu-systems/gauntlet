--- before_pass
+++ after_pass
@@ -31,7 +31,6 @@ struct headers {
     ethernet_t ethernet;
     ipv4_t     ipv4;
 }
-const bit<32> NUM_PORTS = 32w512;
 typedef bit<80> PacketByteCountState_t;
 action update_pkt_ip_byte_count(inout PacketByteCountState_t s, in bit<16> ip_length_bytes) {
     s[79:48] = s[79:48] + 32w1;
