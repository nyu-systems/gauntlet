--- before_pass
+++ after_pass
@@ -208,11 +208,11 @@ control ingress(inout headers_t hdr, ino
             hdr.custom.s0      : exact;
         }
         actions = {
-            set_output;
-            set_headers;
-            my_drop;
+            set_output();
+            set_headers();
+            my_drop();
         }
-        default_action = my_drop;
+        default_action = my_drop();
     }
     apply {
         if (hdr.custom.isValid()) {
