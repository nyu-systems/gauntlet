--- before_pass
+++ after_pass
@@ -24,7 +24,7 @@ control ComputeChecksumI(inout H hdr, in
 control aux(inout M meta, in H hdr) {
     table adjust_lkp_fields {
         key = {
-            hdr.ipv4.isValid(): exact;
+            hdr.ipv4.isValid(): exact @name("hdr.ipv4.$valid$") ;
         }
         actions = {
             NoAction();
