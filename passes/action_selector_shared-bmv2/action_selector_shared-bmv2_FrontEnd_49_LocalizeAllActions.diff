--- before_pass
+++ after_pass
@@ -11,7 +11,14 @@ parser ParserI(packet_in pk, out H hdr,
     }
 }
 control IngressI(inout H hdr, inout M meta, inout standard_metadata_t smeta) {
-    @name("drop") action drop() {
+    @name(".NoAction") action NoAction_1() {
+    }
+    @name(".NoAction") action NoAction_2() {
+    }
+    @name("drop") action drop_0() {
+        mark_to_drop(smeta);
+    }
+    @name("drop") action drop_1() {
         mark_to_drop(smeta);
     }
     @name("as") action_selector(HashAlgorithm.identity, 32w1024, 32w10) as;
@@ -20,10 +27,10 @@ control IngressI(inout H hdr, inout M me
             meta.hash1: selector @name("meta.hash1") ;
         }
         actions = {
-            drop();
-            NoAction();
+            drop_0();
+            NoAction_1();
         }
-        const default_action = NoAction();
+        const default_action = NoAction_1();
         @name("ap_ws") implementation = as;
     }
     @name("indirect_ws_1") table indirect_ws_1 {
@@ -31,10 +38,10 @@ control IngressI(inout H hdr, inout M me
             meta.hash1: selector @name("meta.hash1") ;
         }
         actions = {
-            drop();
-            NoAction();
+            drop_1();
+            NoAction_2();
         }
-        const default_action = NoAction();
+        const default_action = NoAction_2();
         @name("ap_ws") implementation = as;
     }
     apply {
