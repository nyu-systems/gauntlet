--- before_pass
+++ after_pass
@@ -12,7 +12,9 @@ parser ParserI(packet_in pk, out H hdr,
 }
 control IngressI(inout H hdr, inout M meta, inout standard_metadata_t smeta) {
     @name("drop") action drop_0() {
-        mark_to_drop(smeta);
+        {
+            mark_to_drop(smeta);
+        }
     }
     @name("as") action_selector(HashAlgorithm.identity, 32w1024, 32w10) as_0;
     @name("indirect_ws") table indirect_ws_0 {
@@ -38,8 +40,12 @@ control IngressI(inout H hdr, inout M me
         @name("ap_ws") implementation = as_0;
     }
     apply {
-        indirect_ws_0.apply();
-        indirect_ws_2.apply();
+        {
+            indirect_ws_0.apply();
+        }
+        {
+            indirect_ws_2.apply();
+        }
     }
 }
 control EgressI(inout H hdr, inout M meta, inout standard_metadata_t smeta) {
