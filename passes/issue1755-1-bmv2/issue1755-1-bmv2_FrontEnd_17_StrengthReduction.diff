--- before_pass
+++ after_pass
@@ -12,7 +12,7 @@ parser MyParser(packet_in packet, out he
     state start {
         packet.advance(32w8);
         packet.extract<H>(hdr.byte);
-        packet.advance((bit<32>)(hdr.byte.byte * 8w8));
+        packet.advance((bit<32>)(hdr.byte.byte << 3));
         transition accept;
     }
 }
