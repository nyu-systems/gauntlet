--- before_pass
+++ after_pass
@@ -6,8 +6,8 @@ parser p1(packet_in p, out Header h) {
     bit<1> x;
     state start {
         transition select(x) {
-            0: chain1;
-            1: chain2;
+            1w0: chain1;
+            1w1: chain2;
         }
     }
     state chain1 {
