--- before_pass
+++ after_pass
@@ -3,9 +3,9 @@ header Header {
     bit<32> data;
 }
 parser p1(packet_in p, out Header h) {
-    bit<1> x;
+    bit<1> x_0;
     state start {
-        transition select(x) {
+        transition select(x_0) {
             1w0: chain1;
             1w1: chain2;
         }
