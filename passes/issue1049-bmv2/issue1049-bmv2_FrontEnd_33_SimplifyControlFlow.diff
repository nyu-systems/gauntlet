--- before_pass
+++ after_pass
@@ -70,7 +70,6 @@ control cIngress(inout headers hdr, inou
     }
     apply {
         if (hdr.ipv4.isValid()) {
-            ;
             guh_0.apply();
             debug_table_0.apply();
             if (meta.mystruct1.hash_drop) {
