--- before_pass
+++ after_pass
@@ -7,11 +7,9 @@ control c(inout bit<32> r) {
     bit<32> tmp;
     bit<32> tmp_0;
     apply {
-        {
-            tmp = f(32w2);
-            tmp_0 = tmp;
-            h_0[tmp_0].setValid();
-        }
+        tmp = f(32w2);
+        tmp_0 = tmp;
+        h_0[tmp_0].setValid();
     }
 }
 control simple(inout bit<32> r);
