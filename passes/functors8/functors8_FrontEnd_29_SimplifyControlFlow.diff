--- before_pass
+++ after_pass
@@ -7,10 +7,8 @@ parser p1<T>(out T a) {
     @name("ei") e<T>() ei_0;
     T tmp;
     state start {
-        {
-            tmp = ei_0.get();
-            a = tmp;
-        }
+        tmp = ei_0.get();
+        a = tmp;
         transition accept;
     }
 }
