--- before_pass
+++ after_pass
@@ -5,8 +5,12 @@ extern e<T> {
 }
 parser p1<T>(out T a) {
     @name("ei") e<T>() ei_0;
+    T tmp;
     state start {
-        a = ei_0.get();
+        {
+            tmp = ei_0.get();
+            a = tmp;
+        }
         transition accept;
     }
 }
