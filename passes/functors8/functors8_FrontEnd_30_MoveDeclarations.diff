--- before_pass
+++ after_pass
@@ -4,8 +4,8 @@ extern e<T> {
     T get();
 }
 parser p1<T>(out T a) {
-    @name("ei") e<T>() ei_0;
     T tmp;
+    @name("ei") e<T>() ei_0;
     state start {
         tmp = ei_0.get();
         a = tmp;
