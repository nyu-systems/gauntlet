--- before_pass
+++ after_pass
@@ -3,15 +3,15 @@ extern e<T> {
     e();
     T get();
 }
-parser p1<T>(out T a) {
-    T tmp;
-    @name("ei") e<T>() ei_0;
+parser simple(out bit<2> a);
+package m(simple n);
+parser p1_0(out bit<2> a) {
+    bit<2> tmp;
+    @name("ei") e<bit<2>>() ei_0;
     state start {
         tmp = ei_0.get();
         a = tmp;
         transition accept;
     }
 }
-parser simple(out bit<2> a);
-package m(simple n);
-m(p1<bit<2>>()) main;
+m(p1_0()) main;
