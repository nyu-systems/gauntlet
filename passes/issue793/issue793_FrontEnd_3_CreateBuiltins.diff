--- before_pass
+++ after_pass
@@ -13,11 +13,13 @@ control c(in my_packet hdr) {
     }
     table t {
         actions = {
-            nop;
+            nop();
+            @defaultonly NoAction();
         }
         key = {
             hdr.data.db: exact;
         }
+        default_action = NoAction();
     }
     apply {
         if (hdr.data.da == 32w1) {
