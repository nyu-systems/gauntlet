--- before_pass
+++ after_pass
@@ -9,11 +9,11 @@ struct my_packet {
     data_h data;
 }
 control c(in my_packet hdr) {
-    @name("nop") action nop_0() {
+    @name("nop") action nop() {
     }
-    @name("t") table t_0 {
+    @name("t") table t {
         actions = {
-            nop_0();
+            nop();
             @defaultonly NoAction();
         }
         key = {
@@ -23,7 +23,7 @@ control c(in my_packet hdr) {
     }
     apply {
         if (hdr.data.da == 32w1) {
-            t_0.apply();
+            t.apply();
         }
     }
 }
