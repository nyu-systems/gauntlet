--- before_pass
+++ after_pass
@@ -17,7 +17,7 @@ control c(in my_packet hdr) {
             @defaultonly NoAction();
         }
         key = {
-            hdr.data.db: exact;
+            hdr.data.db: exact @name("hdr.data.db") ;
         }
         default_action = NoAction();
     }
