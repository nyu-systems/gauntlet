--- before_pass
+++ after_pass
@@ -13,6 +13,6 @@ struct hdrs {
 }
 control test(inout hdrs hdr) {
     apply {
-        hdr.crc = crc_poly<bit<16>>(16w0x801a).hash(hdr.h1);
+        hdr.crc = crc_poly<bit<16>>(16w0x801a).hash<h1_t>(hdr.h1);
     }
 }
