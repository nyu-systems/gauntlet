--- before_pass
+++ after_pass
@@ -14,9 +14,9 @@ struct tuple_0 {
 }
 extern bit<16> get<T>(in T data);
 control cc() {
-    headers hdr;
+    headers hdr_0;
     apply {
-        get<headers>(headers {ipv4_option_timestamp = hdr.ipv4_option_timestamp});
+        get<headers>(headers {ipv4_option_timestamp = hdr_0.ipv4_option_timestamp});
     }
 }
 control C();
