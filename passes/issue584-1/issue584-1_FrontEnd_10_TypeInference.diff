--- before_pass
+++ after_pass
@@ -6,7 +6,7 @@ package top(p _p);
 control c() {
     apply {
         bit<16> var;
-        bit<32> hdr = 0;
+        bit<32> hdr = (bit<32>)32w0;
         hash(var, HashAlgorithm.crc16, (Hash)0, hdr, (Hash)0xffff);
     }
 }
