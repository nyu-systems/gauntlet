--- before_pass
+++ after_pass
@@ -48,19 +48,19 @@ control deparser(packet_out b, in Header
     }
 }
 control ingress(inout Headers h, inout Meta m, inout standard_metadata_t sm) {
-    @name("a") action a_0() {
+    @name("a") action a_1() {
     }
-    @name("t") table t {
+    @name("t") table t_0 {
         key = {
             h.u.isValid(): exact @name("h.u.$valid$") ;
         }
         actions = {
-            a_0();
+            a_1();
         }
-        default_action = a_0();
+        default_action = a_1();
     }
     apply {
-        t.apply();
+        t_0.apply();
     }
 }
 V1Switch<Headers, Meta>(p(), vrfy(), ingress(), egress(), update(), deparser()) main;
