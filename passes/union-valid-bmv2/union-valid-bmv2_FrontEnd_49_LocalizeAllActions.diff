--- before_pass
+++ after_pass
@@ -48,16 +48,16 @@ control deparser(packet_out b, in Header
     }
 }
 control ingress(inout Headers h, inout Meta m, inout standard_metadata_t sm) {
-    @name("a") action a_1() {
+    @name("a") action a_0() {
     }
     @name("t") table t {
         key = {
             h.u.isValid(): exact @name("h.u.$valid$") ;
         }
         actions = {
-            a_1();
+            a_0();
         }
-        default_action = a_1();
+        default_action = a_0();
     }
     apply {
         t.apply();
