--- before_pass
+++ after_pass
@@ -30,7 +30,7 @@ control cIngress(inout Parsed_packet hdr
             hdr.ethernet.srcAddr = 48w0xdeadbeeff00d;
             return;
         }
-        hdr.ethernet.srcAddr = ~48w0xdeadbeeff00d;
+        hdr.ethernet.srcAddr = 48w0x215241100ff2;
     }
     table tbl1 {
         key = {
