--- before_pass
+++ after_pass
@@ -25,24 +25,24 @@ parser parserI(packet_in pkt, out Parsed
     }
 }
 control cIngress(inout Parsed_packet hdr, inout mystruct1 meta, inout standard_metadata_t stdmeta) {
-    action foo(bit<16> bar) {
+    @name("foo") action foo_0(bit<16> bar) {
         if (bar == 16w0xf00d) {
             hdr.ethernet.srcAddr = 48w0xdeadbeeff00d;
             return;
         }
         hdr.ethernet.srcAddr = 48w0x215241100ff2;
     }
-    table tbl1 {
+    @name("tbl1") table tbl1_0 {
         key = {
         }
         actions = {
-            foo();
+            foo_0();
             NoAction();
         }
         default_action = NoAction();
     }
     apply {
-        tbl1.apply();
+        tbl1_0.apply();
         return;
     }
 }
