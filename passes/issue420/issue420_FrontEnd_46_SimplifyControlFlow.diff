--- before_pass
+++ after_pass
@@ -29,9 +29,7 @@ control cIngress(inout Parsed_packet hdr
         bool hasReturned = false;
         if (bar == 16w0xf00d) {
             hdr.ethernet.srcAddr = 48w0xdeadbeeff00d;
-            {
-                hasReturned = true;
-            }
+            hasReturned = true;
         }
         if (!hasReturned) {
             hdr.ethernet.srcAddr = 48w0x215241100ff2;
@@ -49,9 +47,7 @@ control cIngress(inout Parsed_packet hdr
     apply {
         bool hasReturned_0 = false;
         tbl1_0.apply();
-        {
-            hasReturned_0 = true;
-        }
+        hasReturned_0 = true;
     }
 }
 control cEgress(inout Parsed_packet hdr, inout mystruct1 meta, inout standard_metadata_t stdmeta) {
