--- before_pass
+++ after_pass
@@ -25,7 +25,9 @@ parser parserI(packet_in pkt, out Parsed
     }
 }
 control cIngress(inout Parsed_packet hdr, inout mystruct1 meta, inout standard_metadata_t stdmeta) {
-    @name("foo") action foo(bit<16> bar) {
+    @name(".NoAction") action NoAction_1() {
+    }
+    @name("foo") action foo_0(bit<16> bar) {
         bool hasReturned_1 = false;
         if (bar == 16w0xf00d) {
             hdr.ethernet.srcAddr = 48w0xdeadbeeff00d;
@@ -39,10 +41,10 @@ control cIngress(inout Parsed_packet hdr
         key = {
         }
         actions = {
-            foo();
-            NoAction();
+            foo_0();
+            NoAction_1();
         }
-        default_action = NoAction();
+        default_action = NoAction_1();
     }
     apply {
         bool hasReturned_2 = false;
