--- before_pass
+++ after_pass
@@ -1,6 +1,6 @@
 #include <core.p4>
 parser p(out bit<32> b) {
-    bit<32> a = (bit<32>)32w1;
+    bit<32> a = 32w1;
     state start {
         b = (a == 32w0 ? 32w2 : 32w3);
         b = b + 32w1;
