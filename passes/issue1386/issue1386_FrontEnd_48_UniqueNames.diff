--- before_pass
+++ after_pass
@@ -32,16 +32,16 @@ control deparser(packet_out b, in Header
     }
 }
 control ingress(inout Headers h, inout Meta m, inout standard_metadata_t sm) {
-    bit<8> c_n;
+    bit<8> c_n_0;
     apply {
         {
-            bool c_hasReturned = false;
-            c_n = 8w0;
+            bool c_hasReturned_0 = false;
+            c_n_0 = 8w0;
             if (!h.h.isValid()) {
-                c_hasReturned = true;
+                c_hasReturned_0 = true;
             }
-            if (!c_hasReturned) {
-                if (c_n > 8w0) {
+            if (!c_hasReturned_0) {
+                if (c_n_0 > 8w0) {
                     h.h.setValid();
                 }
             }
