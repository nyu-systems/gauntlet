--- before_pass
+++ after_pass
@@ -4,12 +4,12 @@ header hdr {
     bit<32> a;
 }
 control compute(inout hdr h) {
-    bit<8> n = 8w0;
+    bit<8> n_0 = 8w0;
     apply {
         if (!h.isValid()) {
             return;
         }
-        if (n > 8w0) {
+        if (n_0 > 8w0) {
             h.setValid();
         }
     }
@@ -43,9 +43,9 @@ control deparser(packet_out b, in Header
     }
 }
 control ingress(inout Headers h, inout Meta m, inout standard_metadata_t sm) {
-    compute() c;
+    @name("c") compute() c_0;
     apply {
-        c.apply(h.h);
+        c_0.apply(h.h);
         sm.egress_spec = 9w0;
     }
 }
