--- before_pass
+++ after_pass
@@ -11,8 +11,8 @@ parser p(packet_in b, out Headers h, ino
 }
 register<bit<8>>(32w2) r;
 control ingress(inout Headers h, inout Meta m, inout standard_metadata_t sm) {
+    bit<8> x_0;
     apply {
-        bit<8> x_0;
         r.read(x_0, 32w0);
     }
 }
