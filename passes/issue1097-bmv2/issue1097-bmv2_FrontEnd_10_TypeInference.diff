--- before_pass
+++ after_pass
@@ -9,11 +9,11 @@ parser p(packet_in b, out Headers h, ino
         transition accept;
     }
 }
-register<bit<8>>(2) r;
+register<bit<8>>(32w2) r;
 control ingress(inout Headers h, inout Meta m, inout standard_metadata_t sm) {
     apply {
         bit<8> x;
-        r.read(x, 0);
+        r.read(x, 32w0);
     }
 }
 control vrfy(inout Headers h, inout Meta m) {
