--- before_pass
+++ after_pass
@@ -24,7 +24,7 @@ parser parserI(packet_in pkt, out Parsed
     }
 }
 control cIngress(inout Parsed_packet hdr, inout metadata m, inout standard_metadata_t stdmeta) {
-    bit<32> z_0 = 32w5;
+    bit<32> z_0;
     @name("foo") action foo_0() {
     }
     @name("t") table t_0 {
@@ -40,6 +40,7 @@ control cIngress(inout Parsed_packet hdr
         default_action = foo_0();
     }
     apply {
+        z_0 = 32w5;
         t_0.apply();
     }
 }
