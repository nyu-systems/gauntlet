--- before_pass
+++ after_pass
@@ -30,10 +30,10 @@ control cIngress(inout Parsed_packet hdr
     }
     table t {
         key = {
-            hdr.x.add1: exact;
-            m.x.add2  : exact;
-            m.z       : exact;
-            z         : exact;
+            hdr.x.add1: exact @name("hdr.x.add1") ;
+            m.x.add2  : exact @name("m.x.add2") ;
+            m.z       : exact @name("m.z") ;
+            z         : exact @name("z") ;
         }
         actions = {
             foo();
