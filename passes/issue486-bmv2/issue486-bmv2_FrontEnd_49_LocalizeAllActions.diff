--- before_pass
+++ after_pass
@@ -25,7 +25,7 @@ parser parserI(packet_in pkt, out Parsed
 }
 control cIngress(inout Parsed_packet hdr, inout metadata m, inout standard_metadata_t stdmeta) {
     bit<32> z_1;
-    @name("foo") action foo() {
+    @name("foo") action foo_0() {
     }
     @name("t") table t {
         key = {
@@ -35,9 +35,9 @@ control cIngress(inout Parsed_packet hdr
             z_1       : exact @name("z") ;
         }
         actions = {
-            foo();
+            foo_0();
         }
-        default_action = foo();
+        default_action = foo_0();
     }
     apply {
         z_1 = 32w5;
