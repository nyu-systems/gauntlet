--- before_pass
+++ after_pass
@@ -36,9 +36,9 @@ control cIngress(inout Parsed_packet hdr
             z         : exact;
         }
         actions = {
-            foo;
+            foo();
         }
-        default_action = foo;
+        default_action = foo();
     }
     apply {
         t.apply();
