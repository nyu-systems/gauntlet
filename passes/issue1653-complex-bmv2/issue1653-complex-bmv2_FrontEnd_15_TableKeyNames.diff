--- before_pass
+++ after_pass
@@ -49,8 +49,8 @@ control ingress(inout parsed_packet_t h,
     }
     table tns {
         key = {
-            h.bvh1.row.alt1.valid         : exact;
-            local_metadata.row0.alt0.valid: exact;
+            h.bvh1.row.alt1.valid         : exact @name("h.bvh1.row.alt1.valid") ;
+            local_metadata.row0.alt0.valid: exact @name("local_metadata.row0.alt0.valid") ;
         }
         actions = {
             do_act();
