--- before_pass
+++ after_pass
@@ -23,7 +23,7 @@ parser GenericParser(packet_in b, out Pa
         }
     }
     state ipv4 {
-        b.extract(p.ipv4);
+        b.extract<IPv4>(p.ipv4);
         transition select(p.ipv4.protocol) {
             16w6: tryudp;
             16w17: tcp;
