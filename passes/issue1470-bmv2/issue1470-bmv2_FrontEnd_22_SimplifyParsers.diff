--- before_pass
+++ after_pass
@@ -27,9 +27,6 @@ struct headers_t {
 }
 parser InnerParser(packet_in pkt, out headers_t hdr) {
     state start {
-        transition parse_eth;
-    }
-    state parse_eth {
         pkt.extract<eth_h>(hdr.eth);
         transition select(hdr.eth.type) {
             16w0x800: parse_ipv4;
