--- before_pass
+++ after_pass
@@ -32,7 +32,7 @@ parser InnerParser(packet_in pkt, out he
     state parse_eth {
         pkt.extract(hdr.eth);
         transition select(hdr.eth.type) {
-            0x800: parse_ipv4;
+            16w0x800: parse_ipv4;
             default: accept;
         }
     }
