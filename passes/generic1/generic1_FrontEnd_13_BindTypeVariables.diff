--- before_pass
+++ after_pass
@@ -8,12 +8,12 @@ control c<T>()(T size) {
     Generic<T>(size) x;
     apply {
         bit<32> a = x.get<bit<32>>();
-        bit<5> b = x.get1(10w0, 5w0);
-        f(b);
+        bit<5> b = x.get1<bit<5>, bit<10>>(10w0, 5w0);
+        f<bit<5>>(b);
     }
 }
 control caller() {
-    c(8w9) cinst;
+    c<bit<8>>(8w9) cinst;
     apply {
         cinst.apply();
     }
