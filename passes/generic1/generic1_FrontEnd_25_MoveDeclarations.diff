--- before_pass
+++ after_pass
@@ -5,10 +5,12 @@ extern Generic<T> {
 }
 extern void f<T>(in T arg);
 control c<T>()(T size) {
+    bit<32> a_0;
+    bit<5> b_0;
     @name("x") Generic<T>(size) x_0;
     apply {
-        bit<32> a_0 = x_0.get<bit<32>>();
-        bit<5> b_0 = x_0.get1<bit<5>, bit<10>>(10w0, 5w0);
+        a_0 = x_0.get<bit<32>>();
+        b_0 = x_0.get1<bit<5>, bit<10>>(10w0, 5w0);
         f<bit<5>>(b_0);
     }
 }
