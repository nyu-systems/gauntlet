--- before_pass
+++ after_pass
@@ -6,9 +6,9 @@ control c(out H[2] h);
 package top(c _c);
 control my(out H[2] s) {
     apply {
-        bit<32> a = (bit<32>)32w0;
-        s[a].z = (bit<1>)1w1;
-        s[a + 32w1].z = (bit<1>)1w0;
+        bit<32> a = 32w0;
+        s[a].z = 1w1;
+        s[a + 32w1].z = 1w0;
         a = f(s[a].z, 1w0);
         a = f(s[a].z, 1w1);
     }
