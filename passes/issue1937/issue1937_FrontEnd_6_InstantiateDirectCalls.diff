--- before_pass
+++ after_pass
@@ -10,9 +10,11 @@ parser foo(out bit<8> x, in bit<8> y=5)
     }
 }
 parser parserImpl(out h1_t hdr) {
+    @name("foo") foo() foo_inst;
+    @name("foo") foo() foo_inst_0;
     state start {
-        foo.apply(hdr.f1, hdr.f1);
-        foo.apply(hdr.f2);
+        foo_inst.apply(hdr.f1, hdr.f1);
+        foo_inst_0.apply(hdr.f2);
         transition accept;
     }
 }
