--- before_pass
+++ after_pass
@@ -20,7 +20,7 @@ control IngressImpl(inout headers hdr, i
     }
     table debug_table {
         key = {
-            meta.test: exact;
+            meta.test: exact @name("meta.test") ;
         }
         actions = {
             drop();
