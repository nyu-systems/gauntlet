--- before_pass
+++ after_pass
@@ -23,9 +23,11 @@ control IngressImpl(inout headers hdr, i
             meta.test: exact;
         }
         actions = {
-            drop;
-            forward;
+            drop();
+            forward();
+            @defaultonly NoAction();
         }
+        default_action = NoAction();
     }
     apply {
         bit<1> registerData;
